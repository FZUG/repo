# NetData Configuration
# You can uncomment and change any of the options below.
# The value shown in the commented settings, is the default value.

# To see defaults, grab one from your instance:
# http://localhost:19999/netdata.conf

# global netdata configuration

[global]
	# run as user = netdata
	web files owner = root
	web files group = netdata
	# config directory = /etc/netdata
	# plugins directory = /usr/libexec/netdata/plugins.d
	# web files directory = /usr/share/netdata/web
	# cache directory = /var/cache/netdata
	# lib directory = /var/lib/netdata
	# log directory = /var/log/netdata
	# host access prefix =
	# home directory = /var/cache/netdata
	# debug flags = 0x00000000
	# memory deduplication (ksm) = yes
	# debug log = /var/log/netdata/debug.log
	# error log = /var/log/netdata/error.log
	# errors flood protection period = 1200
	# errors to trigger flood protection = 200
	# access log = /var/log/netdata/access.log
	# memory mode = save
	# hostname = mosquito.example.com
	# history = 3600
	# update every = 1
	# pthread stack size = 8388608
	# http port listen backlog = 100
	# port = 19999
	# ip version = any
	# bind socket to IP = *
	# disconnect idle web clients after seconds = 60
	# enable web responses gzip compression = yes

[plugins]
	# tc = yes
	# idlejitter = yes
	# proc = yes
	# cgroups = yes
	# nfacct = yes
	# checks = no
	# plugins directory = /usr/libexec/netdata/plugins.d
	# enable running new plugins = yes
	# check for new plugins every = 60
	# node.d = yes
	# charts.d = yes
	# apps = yes

[registry]
	# enabled = no
	# registry db directory = /var/lib/netdata/registry


# per plugin configuration

[plugin:node.d]
	# update every = 1
	# command options =

[plugin:charts.d]
	# update every = 1
	# command options =

[plugin:apps]
	# update every = 1
	# command options =

[plugin:proc:/proc/net/dev:lo]
	# enabled = no

[plugin:proc:/proc/net/dev:fireqos_monitor]
	# enabled = no

[plugin:proc]
	# /proc/net/dev = yes
	# /proc/diskstats = yes
	# /proc/net/snmp = yes
	# /proc/net/snmp6 = yes
	# /proc/net/netstat = yes
	# /proc/net/stat/conntrack = yes
	# /proc/net/ip_vs/stats = yes
	# /proc/net/stat/synproxy = yes
	# /proc/stat = yes
	# /proc/meminfo = yes
	# /proc/vmstat = yes
	# /proc/net/rpc/nfsd = yes
	# /proc/sys/kernel/random/entropy_avail = yes
	# /proc/interrupts = yes
	# /proc/softirqs = yes
	# /proc/loadavg = yes
	# /sys/kernel/mm/ksm = yes
	# netdata server resources = yes

[plugin:idlejitter]
	# loop time in ms = 20

[plugin:cgroups]
	# cgroups plugin resources = yes
	# check for new cgroups every = 10
	# enable cpuacct stat = on demand
	# enable cpuacct usage = on demand
	# enable memory = on demand
	# enable blkio = on demand
	# path to /sys/fs/cgroup/cpuacct = /sys/fs/cgroup/cpuacct
	# path to /sys/fs/cgroup/blkio = /sys/fs/cgroup/blkio
	# path to /sys/fs/cgroup/memory = /sys/fs/cgroup/memory
	# max cgroups to allow = 500
	# max cgroups depth to monitor = 0
	# enable new cgroups detected at run time = yes
	# script to get cgroup names = /usr/libexec/netdata/plugins.d/cgroup-name.sh
	# enable cgroup / = no
	# search for cgroups under / = yes
	# enable cgroup init.scope = yes
	# enable cgroup system.slice = no
	# search for cgroups under system.slice = yes
	# enable cgroup system.slice/rhsmcertd.service = no
	# enable cgroup system.slice/avahi-daemon.service = no
	# enable cgroup system.slice/var-lib-nfs-rpc_pipefs.mount = no
	# enable cgroup system.slice/boot.mount = no
	# enable cgroup system.slice/ksmtuned.service = no
	# enable cgroup system.slice/dbus.service = no
	# enable cgroup system.slice/firewalld.service = no
	# enable cgroup system.slice/system-lvm2/x2dpvscan.slice = no
	# enable cgroup system.slice/sys-kernel-config.mount = no
	# enable cgroup system.slice/lvm2-lvmetad.service = no
	# enable cgroup system.slice/fedora-import-state.service = no
	# enable cgroup system.slice/systemd-vconsole-setup.service = no
	# enable cgroup system.slice/home.mount = no
	# enable cgroup system.slice/livesys-late.service = no
	# enable cgroup system.slice/systemd-binfmt.service = no
	# enable cgroup system.slice/wpa_supplicant.service = no
	# enable cgroup system.slice/akmods-shutdown.service = no
	# enable cgroup system.slice/dev-disk-by/x2did-dm/x2duuid/x2dlvm/x = no
	# enable cgroup system.slice/var-lib-docker-devicemapper.mount = no
	# enable cgroup system.slice/crond.service = no
	# enable cgroup system.slice/systemd-fsck-root.service = no
	# enable cgroup system.slice/run-user-42.mount = no
	# enable cgroup system.slice/-.mount = no
	# enable cgroup system.slice/alsa-state.service = no
	# enable cgroup system.slice/pcscd.service = no
	# enable cgroup system.slice/tmp.mount = no
	# enable cgroup system.slice/accounts-daemon.service = no
	# enable cgroup system.slice/netcf-transaction.service = no
	# enable cgroup system.slice/colord.service = no
	# enable cgroup system.slice/proc-fs-nfsd.mount = no
	# enable cgroup system.slice/atd.service = no
	# enable cgroup system.slice/systemd-user-sessions.service = no
	# enable cgroup system.slice/systemd-journald.service = no
	# enable cgroup system.slice/run-user-1000.mount = no
	# enable cgroup system.slice/sys-fs-fuse-connections.mount = no
	# enable cgroup system.slice/systemd-tmpfiles-setup.service = no
	# enable cgroup system.slice/dev-disk-by/x2duuid-3c319ad8/x2d9f39/ = no
	# enable cgroup system.slice/proc-sys-fs-binfmt_misc.mount = no
	# enable cgroup system.slice/systemd-sysctl.service = no
	# enable cgroup system.slice/chronyd.service = no
	# enable cgroup system.slice/auditd.service = no
	# enable cgroup system.slice/system-systemd/x2dcoredump.slice = no
	# enable cgroup system.slice/udisks2.service = no
	# enable cgroup system.slice/gssproxy.service = no
	# enable cgroup system.slice/lvm2-monitor.service = no
	# enable cgroup system.slice/upower.service = no
	# enable cgroup system.slice/dev-mqueue.mount = no
	# enable cgroup system.slice/systemd-tmpfiles-setup-dev.service = no
	# enable cgroup system.slice/cups.service = no
	# enable cgroup system.slice/packagekit.service = no
	# enable cgroup system.slice/systemd-logind.service = no
	# enable cgroup system.slice/systemd-journal-flush.service = no
	# enable cgroup system.slice/libvirtd.service = no
	# enable cgroup system.slice/systemd-random-seed.service = no
	# enable cgroup system.slice/iio-sensor-proxy.service = no
	# enable cgroup system.slice/dev-dm/x2d0.swap = no
	# enable cgroup system.slice/system-getty.slice = no
	# enable cgroup system.slice/dm-event.service = no
	# enable cgroup system.slice/dev-fedora-swap.swap = no
	# enable cgroup system.slice/ksm.service = no
	# enable cgroup system.slice/livesys.service = no
	# enable cgroup system.slice/system-systemd/x2dbacklight.slice = no
	# enable cgroup system.slice/dev-mapper-fedora/x2dswap.swap = no
	# enable cgroup system.slice/netdata.service = no
	# enable cgroup system.slice/systemd-udevd.service = no
	# enable cgroup system.slice/mcelog.service = no
	# enable cgroup system.slice/preload.service = no
	# enable cgroup system.slice/fprintd.service = no
	# enable cgroup system.slice/tuned.service = no
	# enable cgroup system.slice/sysstat.service = no
	# enable cgroup system.slice/polkit.service = no
	# enable cgroup system.slice/dracut-shutdown.service = no
	# enable cgroup system.slice/fedora-readonly.service = no
	# enable cgroup system.slice/lxc-net.service = no
	# enable cgroup system.slice/networkmanager.service = no
	# enable cgroup system.slice/kmod-static-nodes.service = no
	# enable cgroup system.slice/irqbalance.service = no
	# enable cgroup system.slice/systemd-udev-trigger.service = no
	# enable cgroup system.slice/rsyslog.service = no
	# enable cgroup system.slice/systemd-remount-fs.service = no
	# enable cgroup system.slice/dev-disk-by/x2did-dm/x2dname/x2dfedor = no
	# enable cgroup system.slice/sys-kernel-debug.mount = no
	# enable cgroup system.slice/blk-availability.service = no
	# enable cgroup system.slice/dev-hugepages.mount = no
	# enable cgroup system.slice/smartd.service = no
	# enable cgroup system.slice/gdm.service = no
	# enable cgroup system.slice/docker.service = no
	# enable cgroup system.slice/libstoragemgmt.service = no
	# enable cgroup system.slice/systemd-udev-settle.service = no
	# enable cgroup system.slice/systemd-update-utmp.service = no
	# enable cgroup system.slice/lxc.service = no
	# enable cgroup system.slice/gpm.service = no
	# enable cgroup system.slice/run-user-1000-gvfs.mount = no
	# enable cgroup system.slice/rtkit-daemon.service = no
	# enable cgroup system.slice/system-systemd/x2dfsck.slice = no
	# enable cgroup user.slice = no

[plugin:tc]
	# script to run to get tc values = /usr/libexec/netdata/plugins.d/tc-qos-helper.sh
	# enable new interfaces detected at runtime = yes
	# cleanup unused classes every = 60

[plugin:proc:/sys/kernel/mm/ksm]
	# /sys/kernel/mm/ksm/pages_shared = /sys/kernel/mm/ksm/pages_shared
	# /sys/kernel/mm/ksm/pages_sharing = /sys/kernel/mm/ksm/pages_sharing
	# /sys/kernel/mm/ksm/pages_unshared = /sys/kernel/mm/ksm/pages_unshared
	# /sys/kernel/mm/ksm/pages_volatile = /sys/kernel/mm/ksm/pages_volatile
	# /sys/kernel/mm/ksm/pages_to_scan = /sys/kernel/mm/ksm/pages_to_scan

[plugin:proc:/proc/loadavg]
	# filename to monitor = /proc/loadavg
	# enable load average = yes
	# enable total processes = yes

[plugin:proc:/proc/interrupts]
	# interrupts per core = yes
	# filename to monitor = /proc/interrupts

[plugin:proc:/proc/softirqs]
	# interrupts per core = yes
	# filename to monitor = /proc/softirqs

[plugin:proc:/proc/sys/kernel/random/entropy_avail]
	# filename to monitor = /proc/sys/kernel/random/entropy_avail

[plugin:proc:/proc/net/dev]
	# filename to monitor = /proc/net/dev
	# enable new interfaces detected at runtime = on demand
	# enable ifb interfaces = no
	# bandwidth for all interfaces = on demand
	# packets for all interfaces = on demand
	# errors for all interfaces = on demand
	# drops for all interfaces = on demand
	# fifo for all interfaces = on demand
	# compressed packets for all interfaces = on demand
	# frames, collisions, carrier counters for all interfaces = on demand

[plugin:proc:/proc/net/dev:br0]
	# enabled = on demand
	# bandwidth = on demand
	# packets = on demand
	# errors = on demand
	# drops = on demand
	# fifo = on demand
	# compressed = on demand
	# events = on demand

[plugin:proc:/proc/net/dev:virbr0]
	# enabled = on demand

[plugin:proc:/proc/net/dev:wlp5s0]
	# enabled = on demand

[plugin:proc:/proc/net/dev:virbr0-nic]
	# enabled = on demand

[plugin:proc:/proc/net/dev:enp3s0]
	# enabled = on demand
	# bandwidth = on demand
	# packets = on demand
	# errors = on demand
	# drops = on demand
	# fifo = on demand
	# compressed = on demand
	# events = on demand

[plugin:proc:/proc/net/dev:docker0]
	# enabled = on demand

[plugin:proc:/proc/diskstats]
	# enable new disks detected at runtime = on demand
	# bandwidth for all disks = on demand
	# operations for all disks = on demand
	# merged operations for all disks = on demand
	# i/o time for all disks = on demand
	# queued operations for all disks = on demand
	# utilization percentage for all disks = on demand
	# backlog for all disks = on demand
	# filename to monitor = /proc/diskstats
	# path to get h/w sector size = /sys/block/%s/queue/hw_sector_size
	# path to get block device partition = /sys/dev/block/%lu:%lu/partition

[plugin:proc:/proc/diskstats:sda]
	# enabled = on demand
	# bandwidth = on demand
	# operations = on demand
	# merged operations = on demand
	# i/o time = on demand
	queued operations = yes
	# utilization percentage = on demand
	# backlog = on demand

[plugin:proc:/proc/diskstats:sda1]
	# enabled = no

[plugin:proc:/proc/diskstats:sda2]
	# enabled = no

[plugin:proc:/proc/diskstats:sda3]
	# enabled = no

[plugin:proc:/proc/diskstats:dm-0]
	# enabled = on demand
	# bandwidth = on demand
	# operations = on demand
	# merged operations = on demand
	# i/o time = on demand
	queued operations = yes
	# utilization percentage = on demand
	# backlog = on demand

[plugin:proc:/proc/diskstats:dm-1]
	# enabled = on demand
	# bandwidth = on demand
	# operations = on demand
	# merged operations = on demand
	# i/o time = on demand
	queued operations = yes
	# utilization percentage = on demand
	# backlog = on demand

[plugin:proc:/proc/diskstats:dm-2]
	# enabled = on demand
	# bandwidth = on demand
	# operations = on demand
	# merged operations = on demand
	# i/o time = on demand
	queued operations = yes
	# utilization percentage = on demand
	# backlog = on demand

[plugin:proc:/proc/diskstats:dm-3]
	# enabled = on demand
	# bandwidth = on demand
	# operations = on demand
	# merged operations = on demand
	# i/o time = on demand
	queued operations = yes
	# utilization percentage = on demand
	# backlog = on demand

[plugin:proc:/proc/diskstats:dm-4]
	# enabled = on demand
	# bandwidth = on demand
	# operations = on demand
	# merged operations = on demand
	# i/o time = on demand
	queued operations = yes
	# utilization percentage = on demand
	# backlog = on demand

[plugin:proc:/proc/diskstats:dm-5]
	# enabled = on demand
	# bandwidth = on demand
	# operations = on demand
	# merged operations = on demand
	# i/o time = on demand
	queued operations = yes
	# utilization percentage = on demand
	# backlog = on demand

[plugin:proc:/proc/net/snmp]
	# ipv4 packets = yes
	# ipv4 fragments sent = yes
	# ipv4 fragments assembly = yes
	# ipv4 errors = yes
	# ipv4 TCP connections = yes
	# ipv4 TCP packets = yes
	# ipv4 TCP errors = yes
	# ipv4 TCP handshake issues = yes
	# ipv4 UDP packets = yes
	# ipv4 UDP errors = yes
	# filename to monitor = /proc/net/snmp

[plugin:proc:/proc/net/snmp6]
	# ipv6 packets = on demand
	# ipv6 fragments sent = on demand
	# ipv6 fragments assembly = on demand
	# ipv6 errors = on demand
	# ipv6 UDP packets = on demand
	# ipv6 UDP errors = on demand
	# ipv6 UDPlite packets = on demand
	# ipv6 UDPlite errors = on demand
	# bandwidth = on demand
	# multicast bandwidth = on demand
	# broadcast bandwidth = on demand
	# multicast packets = on demand
	# icmp = on demand
	# icmp redirects = on demand
	# icmp errors = on demand
	# icmp echos = on demand
	# icmp group membership = on demand
	# icmp router = on demand
	# icmp neighbor = on demand
	# icmp mldv2 = on demand
	# icmp types = on demand
	# ect = on demand
	# filename to monitor = /proc/net/snmp6

[plugin:proc:/proc/net/netstat]
	# bandwidth = on demand
	# input errors = on demand
	# multicast bandwidth = on demand
	# broadcast bandwidth = on demand
	# multicast packets = on demand
	# broadcast packets = on demand
	# filename to monitor = /proc/net/netstat

[plugin:proc:/proc/net/stat/nf_conntrack]
	# netfilter connections = yes
	# netfilter new connections = yes
	# netfilter connection changes = yes
	# netfilter connection expectations = yes
	# netfilter connection searches = yes
	# netfilter errors = yes
	# filename to monitor = /proc/net/stat/nf_conntrack

[plugin:proc:/proc/net/ip_vs_stats]
	# IPVS bandwidth = yes
	# IPVS connections = yes
	# IPVS packets = yes
	# filename to monitor = /proc/net/ip_vs_stats

[plugin:proc:/proc/net/stat/synproxy]
	# SYNPROXY entries = on demand
	# SYNPROXY cookies = on demand
	# SYNPROXY SYN received = on demand
	# SYNPROXY connections reopened = on demand
	# filename to monitor = /proc/net/stat/synproxy

[plugin:proc:/proc/stat]
	# cpu utilization = yes
	# per cpu core utilization = yes
	# cpu interrupts = yes
	# context switches = yes
	# processes started = yes
	# processes running = yes
	# filename to monitor = /proc/stat

[plugin:proc:/proc/meminfo]
	# system ram = yes
	# system swap = yes
	# hardware corrupted ECC = on demand
	# committed memory = yes
	# writeback memory = yes
	# kernel memory = yes
	# slab memory = yes
	# filename to monitor = /proc/meminfo

[plugin:proc:/proc/vmstat]
	# swap i/o = yes
	# disk i/o = yes
	# memory page faults = yes
	# filename to monitor = /proc/vmstat

[plugin:proc:/proc/net/rpc/nfsd]
	# filename to monitor = /proc/net/rpc/nfsd
	# read cache = yes
	# file handles = yes
	# I/O = yes
	# threads = yes
	# read ahead = yes
	# network = yes
	# rpc = yes
	# NFS v2 procedures = yes
	# NFS v3 procedures = yes
	# NFS v4 procedures = yes
	# NFS v4 operations = yes


# per chart configuration

[system.idlejitter]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.idlejitter
	# chart type = line
	# type = system
	# family = processes
	# context = system.idlejitter
	# units = microseconds lost/s
	# priority = 9999
	# gap when lost iterations above = 1
	# name = system.idlejitter
	# title = CPU Idle Jitter (system.idlejitter)
	# dim jitter name = jitter
	# dim jitter algorithm = absolute
	# dim jitter multiplier = 1
	# dim jitter divisor = 1

[netdata.plugin_tc_cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_tc_cpu
	# chart type = stacked
	# type = netdata
	# family = tc.helper
	# context = netdata.plugin_tc_cpu
	# units = milliseconds/s
	# priority = 135000
	# gap when lost iterations above = 1
	# name = netdata.plugin_tc_cpu
	# title = NetData TC CPU usage (netdata.plugin_tc_cpu)
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.plugin_tc_time]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_tc_time
	# chart type = area
	# type = netdata
	# family = tc.helper
	# context = netdata.plugin_tc_time
	# units = milliseconds/run
	# priority = 135001
	# gap when lost iterations above = 1
	# name = netdata.plugin_tc_time
	# title = NetData TC script execution (netdata.plugin_tc_time)
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[system.load]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.load
	# chart type = line
	# type = system
	# family = load
	# context = system.load
	# units = load
	# priority = 100
	# gap when lost iterations above = 1
	# name = system.load
	# title = System Load Average (system.load)
	# dim load1 name = load1
	# dim load1 algorithm = absolute
	# dim load1 multiplier = 1
	# dim load1 divisor = 1000
	# dim load5 name = load5
	# dim load5 algorithm = absolute
	# dim load5 multiplier = 1
	# dim load5 divisor = 1000
	# dim load15 name = load15
	# dim load15 algorithm = absolute
	# dim load15 multiplier = 1
	# dim load15 divisor = 1000

[system.active_processes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.active_processes
	# chart type = line
	# type = system
	# family = processes
	# context = system.active_processes
	# units = processes
	# priority = 750
	# gap when lost iterations above = 1
	# name = system.active_processes
	# title = System Active Processes (system.active_processes)
	# dim active name = active
	# dim active algorithm = absolute
	# dim active multiplier = 1
	# dim active divisor = 1

[netdata.apps_cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.apps_cpu
	# chart type = stacked
	# type = netdata
	# family = apps.plugin
	# context = netdata.apps_cpu
	# units = milliseconds/s
	# priority = 140000
	# gap when lost iterations above = 1
	# name = netdata.apps_cpu
	# title = Apps Plugin CPU (netdata.apps_cpu)
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[system.interrupts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.interrupts
	# chart type = stacked
	# type = system
	# family = interrupts
	# context = system.interrupts
	# units = interrupts/s
	# priority = 1000
	# gap when lost iterations above = 1
	# name = system.interrupts
	# title = System interrupts (system.interrupts)
	# dim 0 name = timer_0
	# dim 0 algorithm = incremental
	# dim 0 multiplier = 1
	# dim 0 divisor = 1
	# dim 1 name = i8042_1
	# dim 1 algorithm = incremental
	# dim 1 multiplier = 1
	# dim 1 divisor = 1
	# dim 8 name = rtc0_8
	# dim 8 algorithm = incremental
	# dim 8 multiplier = 1
	# dim 8 divisor = 1
	# dim 9 name = acpi_9
	# dim 9 algorithm = incremental
	# dim 9 multiplier = 1
	# dim 9 divisor = 1
	# dim 12 name = i8042_12
	# dim 12 algorithm = incremental
	# dim 12 multiplier = 1
	# dim 12 divisor = 1
	# dim 16 name = snd_hda_intel:card1_16
	# dim 16 algorithm = incremental
	# dim 16 multiplier = 1
	# dim 16 divisor = 1
	# dim 17 name = ath9k_17
	# dim 17 algorithm = incremental
	# dim 17 multiplier = 1
	# dim 17 divisor = 1
	# dim 23 name = ehci_hcd:usb2_23
	# dim 23 algorithm = incremental
	# dim 23 multiplier = 1
	# dim 23 divisor = 1
	# dim 25 name = ahci[0000:00:1f.2]_25
	# dim 25 algorithm = incremental
	# dim 25 multiplier = 1
	# dim 25 divisor = 1
	# dim 26 name = nvkm_26
	# dim 26 algorithm = incremental
	# dim 26 multiplier = 1
	# dim 26 divisor = 1
	# dim 27 name = mei_me_27
	# dim 27 algorithm = incremental
	# dim 27 multiplier = 1
	# dim 27 divisor = 1
	# dim 28 name = snd_hda_intel:card0_28
	# dim 28 algorithm = incremental
	# dim 28 multiplier = 1
	# dim 28 divisor = 1
	# dim 29 name = enp3s0_29
	# dim 29 algorithm = incremental
	# dim 29 multiplier = 1
	# dim 29 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim SPU name = SPU
	# dim SPU algorithm = incremental
	# dim SPU multiplier = 1
	# dim SPU divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RTR name = RTR
	# dim RTR algorithm = incremental
	# dim RTR multiplier = 1
	# dim RTR divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim TRM name = TRM
	# dim TRM algorithm = incremental
	# dim TRM multiplier = 1
	# dim TRM divisor = 1
	# dim THR name = THR
	# dim THR algorithm = incremental
	# dim THR multiplier = 1
	# dim THR divisor = 1
	# dim DFR name = DFR
	# dim DFR algorithm = incremental
	# dim DFR multiplier = 1
	# dim DFR divisor = 1
	# dim MCE name = MCE
	# dim MCE algorithm = incremental
	# dim MCE multiplier = 1
	# dim MCE divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim ERR name = ERR
	# dim ERR algorithm = incremental
	# dim ERR multiplier = 1
	# dim ERR divisor = 1
	# dim MIS name = MIS
	# dim MIS algorithm = incremental
	# dim MIS multiplier = 1
	# dim MIS divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1
	# dim PIW name = PIW
	# dim PIW algorithm = incremental
	# dim PIW multiplier = 1
	# dim PIW divisor = 1

[netdata.apps_files]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.apps_files
	# chart type = line
	# type = netdata
	# family = apps.plugin
	# context = netdata.apps_files
	# units = files/s
	# priority = 140001
	# gap when lost iterations above = 1
	# name = netdata.apps_files
	# title = Apps Plugin Files (netdata.apps_files)
	# dim files name = files
	# dim files algorithm = incremental
	# dim files multiplier = 1
	# dim files divisor = 1
	# dim pids name = pids
	# dim pids algorithm = absolute
	# dim pids multiplier = 1
	# dim pids divisor = 1
	# dim fds name = fds
	# dim fds algorithm = absolute
	# dim fds multiplier = 1
	# dim fds divisor = 1
	# dim targets name = targets
	# dim targets algorithm = absolute
	# dim targets multiplier = 1
	# dim targets divisor = 1

[cpu.cpu0_interrupts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu0_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# context = cpu.interrupts
	# units = interrupts/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = cpu.cpu0_interrupts
	# title = CPU0 Interrupts (cpu.cpu0_interrupts)
	# dim 0 name = timer_0
	# dim 0 algorithm = incremental
	# dim 0 multiplier = 1
	# dim 0 divisor = 1
	# dim 1 name = i8042_1
	# dim 1 algorithm = incremental
	# dim 1 multiplier = 1
	# dim 1 divisor = 1
	# dim 8 name = rtc0_8
	# dim 8 algorithm = incremental
	# dim 8 multiplier = 1
	# dim 8 divisor = 1
	# dim 9 name = acpi_9
	# dim 9 algorithm = incremental
	# dim 9 multiplier = 1
	# dim 9 divisor = 1
	# dim 12 name = i8042_12
	# dim 12 algorithm = incremental
	# dim 12 multiplier = 1
	# dim 12 divisor = 1
	# dim 16 name = snd_hda_intel:card1_16
	# dim 16 algorithm = incremental
	# dim 16 multiplier = 1
	# dim 16 divisor = 1
	# dim 17 name = ath9k_17
	# dim 17 algorithm = incremental
	# dim 17 multiplier = 1
	# dim 17 divisor = 1
	# dim 23 name = ehci_hcd:usb2_23
	# dim 23 algorithm = incremental
	# dim 23 multiplier = 1
	# dim 23 divisor = 1
	# dim 25 name = ahci[0000:00:1f.2]_25
	# dim 25 algorithm = incremental
	# dim 25 multiplier = 1
	# dim 25 divisor = 1
	# dim 26 name = nvkm_26
	# dim 26 algorithm = incremental
	# dim 26 multiplier = 1
	# dim 26 divisor = 1
	# dim 27 name = mei_me_27
	# dim 27 algorithm = incremental
	# dim 27 multiplier = 1
	# dim 27 divisor = 1
	# dim 28 name = snd_hda_intel:card0_28
	# dim 28 algorithm = incremental
	# dim 28 multiplier = 1
	# dim 28 divisor = 1
	# dim 29 name = enp3s0_29
	# dim 29 algorithm = incremental
	# dim 29 multiplier = 1
	# dim 29 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim SPU name = SPU
	# dim SPU algorithm = incremental
	# dim SPU multiplier = 1
	# dim SPU divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RTR name = RTR
	# dim RTR algorithm = incremental
	# dim RTR multiplier = 1
	# dim RTR divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim TRM name = TRM
	# dim TRM algorithm = incremental
	# dim TRM multiplier = 1
	# dim TRM divisor = 1
	# dim THR name = THR
	# dim THR algorithm = incremental
	# dim THR multiplier = 1
	# dim THR divisor = 1
	# dim DFR name = DFR
	# dim DFR algorithm = incremental
	# dim DFR multiplier = 1
	# dim DFR divisor = 1
	# dim MCE name = MCE
	# dim MCE algorithm = incremental
	# dim MCE multiplier = 1
	# dim MCE divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim ERR name = ERR
	# dim ERR algorithm = incremental
	# dim ERR multiplier = 1
	# dim ERR divisor = 1
	# dim MIS name = MIS
	# dim MIS algorithm = incremental
	# dim MIS multiplier = 1
	# dim MIS divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1
	# dim PIW name = PIW
	# dim PIW algorithm = incremental
	# dim PIW multiplier = 1
	# dim PIW divisor = 1

[apps.cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.cpu
	# chart type = stacked
	# type = apps
	# family = cpu
	# context = apps.cpu
	# units = cpu time %
	# priority = 20001
	# gap when lost iterations above = 1
	# name = apps.cpu
	# title = Apps CPU Time (400% = 4 cores) (apps.cpu)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 100
	# dim other divisor = 100
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 100
	# dim ksmd divisor = 100
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 100
	# dim system divisor = 100
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 100
	# dim wifi divisor = 100
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 100
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 100
	# dim kernel divisor = 100
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 100
	# dim cron divisor = 100
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 100
	# dim log divisor = 100
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 100
	# dim cups divisor = 100
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 100
	# dim gnome divisor = 100
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 100
	# dim X divisor = 100

[apps.mem]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.mem
	# chart type = stacked
	# type = apps
	# family = mem
	# context = apps.mem
	# units = MB
	# priority = 20003
	# gap when lost iterations above = 1
	# name = apps.mem
	# title = Apps Dedicated Memory (w/o shared) (apps.mem)
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 4096
	# dim other divisor = 1048576
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 4096
	# dim ksmd divisor = 1048576
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 4096
	# dim system divisor = 1048576
	# dim wifi name = wifi
	# dim wifi algorithm = absolute
	# dim wifi multiplier = 4096
	# dim wifi divisor = 1048576
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 4096
	# dim netdata divisor = 1048576
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 4096
	# dim kernel divisor = 1048576
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 4096
	# dim cron divisor = 1048576
	# dim log name = log
	# dim log algorithm = absolute
	# dim log multiplier = 4096
	# dim log divisor = 1048576
	# dim cups name = cups
	# dim cups algorithm = absolute
	# dim cups multiplier = 4096
	# dim cups divisor = 1048576
	# dim gnome name = gnome
	# dim gnome algorithm = absolute
	# dim gnome multiplier = 4096
	# dim gnome divisor = 1048576
	# dim X name = X
	# dim X algorithm = absolute
	# dim X multiplier = 4096
	# dim X divisor = 1048576

[cpu.cpu1_interrupts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu1_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# context = cpu.interrupts
	# units = interrupts/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = cpu.cpu1_interrupts
	# title = CPU1 Interrupts (cpu.cpu1_interrupts)
	# dim 0 name = timer_0
	# dim 0 algorithm = incremental
	# dim 0 multiplier = 1
	# dim 0 divisor = 1
	# dim 1 name = i8042_1
	# dim 1 algorithm = incremental
	# dim 1 multiplier = 1
	# dim 1 divisor = 1
	# dim 8 name = rtc0_8
	# dim 8 algorithm = incremental
	# dim 8 multiplier = 1
	# dim 8 divisor = 1
	# dim 9 name = acpi_9
	# dim 9 algorithm = incremental
	# dim 9 multiplier = 1
	# dim 9 divisor = 1
	# dim 12 name = i8042_12
	# dim 12 algorithm = incremental
	# dim 12 multiplier = 1
	# dim 12 divisor = 1
	# dim 16 name = snd_hda_intel:card1_16
	# dim 16 algorithm = incremental
	# dim 16 multiplier = 1
	# dim 16 divisor = 1
	# dim 17 name = ath9k_17
	# dim 17 algorithm = incremental
	# dim 17 multiplier = 1
	# dim 17 divisor = 1
	# dim 23 name = ehci_hcd:usb2_23
	# dim 23 algorithm = incremental
	# dim 23 multiplier = 1
	# dim 23 divisor = 1
	# dim 25 name = ahci[0000:00:1f.2]_25
	# dim 25 algorithm = incremental
	# dim 25 multiplier = 1
	# dim 25 divisor = 1
	# dim 26 name = nvkm_26
	# dim 26 algorithm = incremental
	# dim 26 multiplier = 1
	# dim 26 divisor = 1
	# dim 27 name = mei_me_27
	# dim 27 algorithm = incremental
	# dim 27 multiplier = 1
	# dim 27 divisor = 1
	# dim 28 name = snd_hda_intel:card0_28
	# dim 28 algorithm = incremental
	# dim 28 multiplier = 1
	# dim 28 divisor = 1
	# dim 29 name = enp3s0_29
	# dim 29 algorithm = incremental
	# dim 29 multiplier = 1
	# dim 29 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim SPU name = SPU
	# dim SPU algorithm = incremental
	# dim SPU multiplier = 1
	# dim SPU divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RTR name = RTR
	# dim RTR algorithm = incremental
	# dim RTR multiplier = 1
	# dim RTR divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim TRM name = TRM
	# dim TRM algorithm = incremental
	# dim TRM multiplier = 1
	# dim TRM divisor = 1
	# dim THR name = THR
	# dim THR algorithm = incremental
	# dim THR multiplier = 1
	# dim THR divisor = 1
	# dim DFR name = DFR
	# dim DFR algorithm = incremental
	# dim DFR multiplier = 1
	# dim DFR divisor = 1
	# dim MCE name = MCE
	# dim MCE algorithm = incremental
	# dim MCE multiplier = 1
	# dim MCE divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim ERR name = ERR
	# dim ERR algorithm = incremental
	# dim ERR multiplier = 1
	# dim ERR divisor = 1
	# dim MIS name = MIS
	# dim MIS algorithm = incremental
	# dim MIS multiplier = 1
	# dim MIS divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1
	# dim PIW name = PIW
	# dim PIW algorithm = incremental
	# dim PIW multiplier = 1
	# dim PIW divisor = 1

[cpu.cpufreq]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpufreq
	# chart type = line
	# type = cpu
	# family = cpufreq
	# context = cpu.cpufreq
	# units = MHz
	# priority = 10001
	# gap when lost iterations above = 1
	# name = cpu.cpufreq
	# title = CPU Clock (cpu.cpufreq)
	# dim cpu0 name = cpu0
	# dim cpu0 algorithm = absolute
	# dim cpu0 multiplier = 1
	# dim cpu0 divisor = 1000
	# dim cpu1 name = cpu1
	# dim cpu1 algorithm = absolute
	# dim cpu1 multiplier = 1
	# dim cpu1 divisor = 1000
	# dim cpu2 name = cpu2
	# dim cpu2 algorithm = absolute
	# dim cpu2 multiplier = 1
	# dim cpu2 divisor = 1000
	# dim cpu3 name = cpu3
	# dim cpu3 algorithm = absolute
	# dim cpu3 multiplier = 1
	# dim cpu3 divisor = 1000

[cgroup_init.scope.cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.cpu
	# chart type = stacked
	# type = cgroup_init.scope
	# family = cpu
	# context = cgroup.cpu
	# units = %
	# priority = 40000
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.cpu
	# title = CPU Usage for cgroup init.scope (cgroup_init.scope.cpu)
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 100
	# dim user divisor = 100
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 100
	# dim system divisor = 100

[cgroup_init.scope.cpu_per_core]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.cpu_per_core
	# chart type = stacked
	# type = cgroup_init.scope
	# family = cpu
	# context = cgroup.cpu_per_core
	# units = %
	# priority = 40100
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.cpu_per_core
	# title = CPU Usage Per Core for cgroup init.scope (cgroup_init.scope.cpu_per_core)
	# dim cpu0 name = cpu0
	# dim cpu0 algorithm = incremental
	# dim cpu0 multiplier = 100
	# dim cpu0 divisor = 1000000
	# dim cpu1 name = cpu1
	# dim cpu1 algorithm = incremental
	# dim cpu1 multiplier = 100
	# dim cpu1 divisor = 1000000
	# dim cpu2 name = cpu2
	# dim cpu2 algorithm = incremental
	# dim cpu2 multiplier = 100
	# dim cpu2 divisor = 1000000
	# dim cpu3 name = cpu3
	# dim cpu3 algorithm = incremental
	# dim cpu3 multiplier = 100
	# dim cpu3 divisor = 1000000

[example.random]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/example.random
	# chart type = stacked
	# type = example
	# family = random
	# context = random
	# units = % of random numbers
	# priority = 150000
	# gap when lost iterations above = 1
	# name = example.random
	# title = Random Numbers Stacked Chart (example.random)
	# dim random1 name = random1
	# dim random1 algorithm = percentage-of-absolute-row
	# dim random1 multiplier = 1
	# dim random1 divisor = 1
	# dim random2 name = random2
	# dim random2 algorithm = percentage-of-absolute-row
	# dim random2 multiplier = 1
	# dim random2 divisor = 1
	# dim random3 name = random3
	# dim random3 algorithm = percentage-of-absolute-row
	# dim random3 multiplier = 1
	# dim random3 divisor = 1

[example.random2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/example.random2
	# chart type = area
	# type = example
	# family = random
	# context = random
	# units = random number
	# priority = 150001
	# gap when lost iterations above = 1
	# name = example.random2
	# title = A random number (example.random2)
	# dim random name = random
	# dim random algorithm = absolute
	# dim random multiplier = 1
	# dim random divisor = 1

[cpu.cpu2_interrupts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu2_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# context = cpu.interrupts
	# units = interrupts/s
	# priority = 2002
	# gap when lost iterations above = 1
	# name = cpu.cpu2_interrupts
	# title = CPU2 Interrupts (cpu.cpu2_interrupts)
	# dim 0 name = timer_0
	# dim 0 algorithm = incremental
	# dim 0 multiplier = 1
	# dim 0 divisor = 1
	# dim 1 name = i8042_1
	# dim 1 algorithm = incremental
	# dim 1 multiplier = 1
	# dim 1 divisor = 1
	# dim 8 name = rtc0_8
	# dim 8 algorithm = incremental
	# dim 8 multiplier = 1
	# dim 8 divisor = 1
	# dim 9 name = acpi_9
	# dim 9 algorithm = incremental
	# dim 9 multiplier = 1
	# dim 9 divisor = 1
	# dim 12 name = i8042_12
	# dim 12 algorithm = incremental
	# dim 12 multiplier = 1
	# dim 12 divisor = 1
	# dim 16 name = snd_hda_intel:card1_16
	# dim 16 algorithm = incremental
	# dim 16 multiplier = 1
	# dim 16 divisor = 1
	# dim 17 name = ath9k_17
	# dim 17 algorithm = incremental
	# dim 17 multiplier = 1
	# dim 17 divisor = 1
	# dim 23 name = ehci_hcd:usb2_23
	# dim 23 algorithm = incremental
	# dim 23 multiplier = 1
	# dim 23 divisor = 1
	# dim 25 name = ahci[0000:00:1f.2]_25
	# dim 25 algorithm = incremental
	# dim 25 multiplier = 1
	# dim 25 divisor = 1
	# dim 26 name = nvkm_26
	# dim 26 algorithm = incremental
	# dim 26 multiplier = 1
	# dim 26 divisor = 1
	# dim 27 name = mei_me_27
	# dim 27 algorithm = incremental
	# dim 27 multiplier = 1
	# dim 27 divisor = 1
	# dim 28 name = snd_hda_intel:card0_28
	# dim 28 algorithm = incremental
	# dim 28 multiplier = 1
	# dim 28 divisor = 1
	# dim 29 name = enp3s0_29
	# dim 29 algorithm = incremental
	# dim 29 multiplier = 1
	# dim 29 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim SPU name = SPU
	# dim SPU algorithm = incremental
	# dim SPU multiplier = 1
	# dim SPU divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RTR name = RTR
	# dim RTR algorithm = incremental
	# dim RTR multiplier = 1
	# dim RTR divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim TRM name = TRM
	# dim TRM algorithm = incremental
	# dim TRM multiplier = 1
	# dim TRM divisor = 1
	# dim THR name = THR
	# dim THR algorithm = incremental
	# dim THR multiplier = 1
	# dim THR divisor = 1
	# dim DFR name = DFR
	# dim DFR algorithm = incremental
	# dim DFR multiplier = 1
	# dim DFR divisor = 1
	# dim MCE name = MCE
	# dim MCE algorithm = incremental
	# dim MCE multiplier = 1
	# dim MCE divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim ERR name = ERR
	# dim ERR algorithm = incremental
	# dim ERR multiplier = 1
	# dim ERR divisor = 1
	# dim MIS name = MIS
	# dim MIS algorithm = incremental
	# dim MIS multiplier = 1
	# dim MIS divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1
	# dim PIW name = PIW
	# dim PIW algorithm = incremental
	# dim PIW multiplier = 1
	# dim PIW divisor = 1

[cgroup_init.scope.mem]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.mem
	# chart type = stacked
	# type = cgroup_init.scope
	# family = mem
	# context = cgroup.mem
	# units = MB
	# priority = 40200
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.mem
	# title = Memory Usage for cgroup init.scope (cgroup_init.scope.mem)
	# dim cache name = cache
	# dim cache algorithm = absolute
	# dim cache multiplier = 1
	# dim cache divisor = 1048576
	# dim rss name = rss
	# dim rss algorithm = absolute
	# dim rss multiplier = 1
	# dim rss divisor = 1048576
	# dim swap name = swap
	# dim swap algorithm = absolute
	# dim swap multiplier = 1
	# dim swap divisor = 1048576
	# dim rss_huge name = rss_huge
	# dim rss_huge algorithm = absolute
	# dim rss_huge multiplier = 1
	# dim rss_huge divisor = 1048576
	# dim mapped_file name = mapped_file
	# dim mapped_file algorithm = absolute
	# dim mapped_file multiplier = 1
	# dim mapped_file divisor = 1048576

[sensors.temp_0000_02_00_0_hwmon_hwmon1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.temp_0000_02_00_0_hwmon_hwmon1
	# chart type = line
	# type = sensors
	# family = temperature
	# context = sensors.temp
	# units = Celsius
	# priority = 90001
	# gap when lost iterations above = 1
	# name = sensors.temp_0000_02_00_0_hwmon_hwmon1
	# title = nouveau Temperature (sensors.temp_0000_02_00_0_hwmon_hwmon1)
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_temp1_input name = temp1
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_temp1_input algorithm = absolute
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_temp1_input multiplier = 1
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_temp1_input divisor = 1000

[apps.threads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.threads
	# chart type = stacked
	# type = apps
	# family = processes
	# context = apps.threads
	# units = threads
	# priority = 20005
	# gap when lost iterations above = 1
	# name = apps.threads
	# title = Apps Threads (apps.threads)
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim wifi name = wifi
	# dim wifi algorithm = absolute
	# dim wifi multiplier = 1
	# dim wifi divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim log name = log
	# dim log algorithm = absolute
	# dim log multiplier = 1
	# dim log divisor = 1
	# dim cups name = cups
	# dim cups algorithm = absolute
	# dim cups multiplier = 1
	# dim cups divisor = 1
	# dim gnome name = gnome
	# dim gnome algorithm = absolute
	# dim gnome multiplier = 1
	# dim gnome divisor = 1
	# dim X name = X
	# dim X algorithm = absolute
	# dim X multiplier = 1
	# dim X divisor = 1

[cgroup_init.scope.writeback]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.writeback
	# chart type = area
	# type = cgroup_init.scope
	# family = mem
	# context = cgroup.writeback
	# units = MB
	# priority = 40300
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.writeback
	# title = Writeback Memory for cgroup init.scope (cgroup_init.scope.writeback)
	# dim dirty name = dirty
	# dim dirty algorithm = absolute
	# dim dirty multiplier = 1
	# dim dirty divisor = 1048576
	# dim writeback name = writeback
	# dim writeback algorithm = absolute
	# dim writeback multiplier = 1
	# dim writeback divisor = 1048576

[sensors.volt_0000_02_00_0_hwmon_hwmon1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.volt_0000_02_00_0_hwmon_hwmon1
	# chart type = line
	# type = sensors
	# family = voltage
	# context = sensors.volt
	# units = Volts
	# priority = 90002
	# gap when lost iterations above = 1
	# name = sensors.volt_0000_02_00_0_hwmon_hwmon1
	# title = nouveau Voltage (sensors.volt_0000_02_00_0_hwmon_hwmon1)
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_in0_input name = GPU core
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_in0_input algorithm = absolute
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_in0_input multiplier = 1
	# dim sys_devices_pci0000_00_0000_00_01_0_0000_02_00_0_hwmon_hwmon1_in0_input divisor = 1000

[cgroup_init.scope.mem_activity]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.mem_activity
	# chart type = line
	# type = cgroup_init.scope
	# family = mem
	# context = cgroup.mem_activity
	# units = MB/s
	# priority = 40400
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.mem_activity
	# title = Memory Activity for cgroup init.scope (cgroup_init.scope.mem_activity)
	# dim pgpgin name = in
	# dim pgpgin algorithm = incremental
	# dim pgpgin multiplier = 4096
	# dim pgpgin divisor = 1048576
	# dim pgpgout name = out
	# dim pgpgout algorithm = incremental
	# dim pgpgout multiplier = -4096
	# dim pgpgout divisor = 1048576

[cgroup_init.scope.pgfaults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.pgfaults
	# chart type = line
	# type = cgroup_init.scope
	# family = mem
	# context = cgroup.pgfaults
	# units = MB/s
	# priority = 40500
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.pgfaults
	# title = Memory Page Faults for cgroup init.scope (cgroup_init.scope.pgfaults)
	# dim pgfault name = pgfault
	# dim pgfault algorithm = incremental
	# dim pgfault multiplier = 4096
	# dim pgfault divisor = 1048576
	# dim pgmajfault name = swap
	# dim pgmajfault algorithm = incremental
	# dim pgmajfault multiplier = -4096
	# dim pgmajfault divisor = 1048576

[cgroup_init.scope.io]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.io
	# chart type = line
	# type = cgroup_init.scope
	# family = disk
	# context = cgroup.io
	# units = KB/s
	# priority = 41200
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.io
	# title = I/O Bandwidth (all disks) for cgroup init.scope (cgroup_init.scope.io)
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1024
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = -1
	# dim write divisor = 1024

[sensors.temp_coretemp_0_hwmon_hwmon2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.temp_coretemp_0_hwmon_hwmon2
	# chart type = line
	# type = sensors
	# family = temperature
	# context = sensors.temp
	# units = Celsius
	# priority = 90001
	# gap when lost iterations above = 1
	# name = sensors.temp_coretemp_0_hwmon_hwmon2
	# title = coretemp Temperature (sensors.temp_coretemp_0_hwmon_hwmon2)
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp2_input name = Core 0
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp2_input algorithm = absolute
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp2_input multiplier = 1
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp2_input divisor = 1000
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp4_input name = Core 2
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp4_input algorithm = absolute
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp4_input multiplier = 1
	# dim sys_devices_platform_coretemp_0_hwmon_hwmon2_temp4_input divisor = 1000

[cgroup_init.scope.serviced_ops]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.serviced_ops
	# chart type = line
	# type = cgroup_init.scope
	# family = disk
	# context = cgroup.serviced_ops
	# units = operations/s
	# priority = 41200
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.serviced_ops
	# title = Serviced I/O Operations (all disks) for cgroup init.scope (cgroup_init.scope.serviced_ops)
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = -1
	# dim write divisor = 1

[apps.processes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.processes
	# chart type = stacked
	# type = apps
	# family = processes
	# context = apps.processes
	# units = processes
	# priority = 20004
	# gap when lost iterations above = 1
	# name = apps.processes
	# title = Apps Processes (apps.processes)
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim wifi name = wifi
	# dim wifi algorithm = absolute
	# dim wifi multiplier = 1
	# dim wifi divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim log name = log
	# dim log algorithm = absolute
	# dim log multiplier = 1
	# dim log divisor = 1
	# dim cups name = cups
	# dim cups algorithm = absolute
	# dim cups multiplier = 1
	# dim cups divisor = 1
	# dim gnome name = gnome
	# dim gnome algorithm = absolute
	# dim gnome multiplier = 1
	# dim gnome divisor = 1
	# dim X name = X
	# dim X algorithm = absolute
	# dim X multiplier = 1
	# dim X divisor = 1

[cgroup_init.scope.throttle_serviced_ops]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.throttle_serviced_ops
	# chart type = line
	# type = cgroup_init.scope
	# family = disk
	# context = cgroup.throttle_serviced_ops
	# units = operations/s
	# priority = 41200
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.throttle_serviced_ops
	# title = Throttle Serviced I/O Operations (all disks) for cgroup init.scope (cgroup_init.scope.throttle_serviced_ops)
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = -1
	# dim write divisor = 1

[cgroup_init.scope.queued_ops]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.queued_ops
	# chart type = line
	# type = cgroup_init.scope
	# family = disk
	# context = cgroup.queued_ops
	# units = operations
	# priority = 42000
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.queued_ops
	# title = Queued I/O Operations (all disks) for cgroup init.scope (cgroup_init.scope.queued_ops)
	# dim read name = read
	# dim read algorithm = absolute
	# dim read multiplier = 1
	# dim read divisor = 1
	# dim write name = write
	# dim write algorithm = absolute
	# dim write multiplier = -1
	# dim write divisor = 1

[cgroup_init.scope.merged_ops]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_init.scope.merged_ops
	# chart type = line
	# type = cgroup_init.scope
	# family = disk
	# context = cgroup.merged_ops
	# units = operations/s
	# priority = 42100
	# gap when lost iterations above = 1
	# name = cgroup_init.scope.merged_ops
	# title = Merged I/O Operations (all disks) for cgroup init.scope (cgroup_init.scope.merged_ops)
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1024
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = -1
	# dim write divisor = 1024

[sensors.temp_hwmon0_hwmon_hwmon0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.temp_hwmon0_hwmon_hwmon0
	# chart type = line
	# type = sensors
	# family = temperature
	# context = sensors.temp
	# units = Celsius
	# priority = 90001
	# gap when lost iterations above = 1
	# name = sensors.temp_hwmon0_hwmon_hwmon0
	# title = acpitz Temperature (sensors.temp_hwmon0_hwmon_hwmon0)
	# dim sys_devices_virtual_hwmon_hwmon0_temp1_input name = temp1
	# dim sys_devices_virtual_hwmon_hwmon0_temp1_input algorithm = absolute
	# dim sys_devices_virtual_hwmon_hwmon0_temp1_input multiplier = 1
	# dim sys_devices_virtual_hwmon_hwmon0_temp1_input divisor = 1000
	# dim sys_devices_virtual_hwmon_hwmon0_temp2_input name = temp2
	# dim sys_devices_virtual_hwmon_hwmon0_temp2_input algorithm = absolute
	# dim sys_devices_virtual_hwmon_hwmon0_temp2_input multiplier = 1
	# dim sys_devices_virtual_hwmon_hwmon0_temp2_input divisor = 1000

[netdata.plugin_cgroups_cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_cgroups_cpu
	# chart type = stacked
	# type = netdata
	# family = proc.internal
	# context = netdata.plugin_cgroups_cpu
	# units = milliseconds/s
	# priority = 132000
	# gap when lost iterations above = 1
	# name = netdata.plugin_cgroups_cpu
	# title = NetData CGroups Plugin CPU usage (netdata.plugin_cgroups_cpu)
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[sensors.temp_lnxtherm_00_thermal_thermal_zone0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.temp_lnxtherm_00_thermal_thermal_zone0
	# chart type = line
	# type = sensors
	# family = temperature
	# context = sensors.temp
	# units = Celsius
	# priority = 90001
	# gap when lost iterations above = 1
	# name = sensors.temp_lnxtherm_00_thermal_thermal_zone0
	# title = thermal_zone0 Temperature (sensors.temp_lnxtherm_00_thermal_thermal_zone0)
	# dim sys_devices_virtual_thermal_thermal_zone0_temp name = temp
	# dim sys_devices_virtual_thermal_thermal_zone0_temp algorithm = absolute
	# dim sys_devices_virtual_thermal_thermal_zone0_temp multiplier = 1
	# dim sys_devices_virtual_thermal_thermal_zone0_temp divisor = 1000

[apps.cpu_user]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.cpu_user
	# chart type = stacked
	# type = apps
	# family = cpu
	# context = apps.cpu_user
	# units = cpu time %
	# priority = 20020
	# gap when lost iterations above = 1
	# name = apps.cpu_user
	# title = Apps CPU User Time (400% = 4 cores) (apps.cpu_user)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 100
	# dim other divisor = 400
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 100
	# dim ksmd divisor = 400
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 100
	# dim system divisor = 400
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 100
	# dim wifi divisor = 400
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 400
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 100
	# dim kernel divisor = 400
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 100
	# dim cron divisor = 400
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 100
	# dim log divisor = 400
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 100
	# dim cups divisor = 400
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 100
	# dim gnome divisor = 400
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 100
	# dim X divisor = 400

[cpu.cpu3_interrupts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu3_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# context = cpu.interrupts
	# units = interrupts/s
	# priority = 2003
	# gap when lost iterations above = 1
	# name = cpu.cpu3_interrupts
	# title = CPU3 Interrupts (cpu.cpu3_interrupts)
	# dim 0 name = timer_0
	# dim 0 algorithm = incremental
	# dim 0 multiplier = 1
	# dim 0 divisor = 1
	# dim 1 name = i8042_1
	# dim 1 algorithm = incremental
	# dim 1 multiplier = 1
	# dim 1 divisor = 1
	# dim 8 name = rtc0_8
	# dim 8 algorithm = incremental
	# dim 8 multiplier = 1
	# dim 8 divisor = 1
	# dim 9 name = acpi_9
	# dim 9 algorithm = incremental
	# dim 9 multiplier = 1
	# dim 9 divisor = 1
	# dim 12 name = i8042_12
	# dim 12 algorithm = incremental
	# dim 12 multiplier = 1
	# dim 12 divisor = 1
	# dim 16 name = snd_hda_intel:card1_16
	# dim 16 algorithm = incremental
	# dim 16 multiplier = 1
	# dim 16 divisor = 1
	# dim 17 name = ath9k_17
	# dim 17 algorithm = incremental
	# dim 17 multiplier = 1
	# dim 17 divisor = 1
	# dim 23 name = ehci_hcd:usb2_23
	# dim 23 algorithm = incremental
	# dim 23 multiplier = 1
	# dim 23 divisor = 1
	# dim 25 name = ahci[0000:00:1f.2]_25
	# dim 25 algorithm = incremental
	# dim 25 multiplier = 1
	# dim 25 divisor = 1
	# dim 26 name = nvkm_26
	# dim 26 algorithm = incremental
	# dim 26 multiplier = 1
	# dim 26 divisor = 1
	# dim 27 name = mei_me_27
	# dim 27 algorithm = incremental
	# dim 27 multiplier = 1
	# dim 27 divisor = 1
	# dim 28 name = snd_hda_intel:card0_28
	# dim 28 algorithm = incremental
	# dim 28 multiplier = 1
	# dim 28 divisor = 1
	# dim 29 name = enp3s0_29
	# dim 29 algorithm = incremental
	# dim 29 multiplier = 1
	# dim 29 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim SPU name = SPU
	# dim SPU algorithm = incremental
	# dim SPU multiplier = 1
	# dim SPU divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RTR name = RTR
	# dim RTR algorithm = incremental
	# dim RTR multiplier = 1
	# dim RTR divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim TRM name = TRM
	# dim TRM algorithm = incremental
	# dim TRM multiplier = 1
	# dim TRM divisor = 1
	# dim THR name = THR
	# dim THR algorithm = incremental
	# dim THR multiplier = 1
	# dim THR divisor = 1
	# dim DFR name = DFR
	# dim DFR algorithm = incremental
	# dim DFR multiplier = 1
	# dim DFR divisor = 1
	# dim MCE name = MCE
	# dim MCE algorithm = incremental
	# dim MCE multiplier = 1
	# dim MCE divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim ERR name = ERR
	# dim ERR algorithm = incremental
	# dim ERR multiplier = 1
	# dim ERR divisor = 1
	# dim MIS name = MIS
	# dim MIS algorithm = incremental
	# dim MIS multiplier = 1
	# dim MIS divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1
	# dim PIW name = PIW
	# dim PIW algorithm = incremental
	# dim PIW multiplier = 1
	# dim PIW divisor = 1

[sensors.temp_lnxtherm_01_thermal_thermal_zone1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.temp_lnxtherm_01_thermal_thermal_zone1
	# chart type = line
	# type = sensors
	# family = temperature
	# context = sensors.temp
	# units = Celsius
	# priority = 90001
	# gap when lost iterations above = 1
	# name = sensors.temp_lnxtherm_01_thermal_thermal_zone1
	# title = thermal_zone1 Temperature (sensors.temp_lnxtherm_01_thermal_thermal_zone1)
	# dim sys_devices_virtual_thermal_thermal_zone1_temp name = temp
	# dim sys_devices_virtual_thermal_thermal_zone1_temp algorithm = absolute
	# dim sys_devices_virtual_thermal_thermal_zone1_temp multiplier = 1
	# dim sys_devices_virtual_thermal_thermal_zone1_temp divisor = 1000

[apps.cpu_system]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.cpu_system
	# chart type = stacked
	# type = apps
	# family = cpu
	# context = apps.cpu_system
	# units = cpu time %
	# priority = 20021
	# gap when lost iterations above = 1
	# name = apps.cpu_system
	# title = Apps CPU System Time (400% = 4 cores) (apps.cpu_system)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 100
	# dim other divisor = 400
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 100
	# dim ksmd divisor = 400
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 100
	# dim system divisor = 400
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 100
	# dim wifi divisor = 400
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 400
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 100
	# dim kernel divisor = 400
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 100
	# dim cron divisor = 400
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 100
	# dim log divisor = 400
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 100
	# dim cups divisor = 400
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 100
	# dim gnome divisor = 400
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 100
	# dim X divisor = 400

[netdata.plugin_chartsd_cpufreq]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_chartsd_cpufreq
	# chart type = area
	# type = netdata
	# family = charts.d
	# context = netdata.plugin_charts
	# units = milliseconds / run
	# priority = 145000
	# gap when lost iterations above = 1
	# name = netdata.plugin_chartsd_cpufreq
	# title = Execution time for cpufreq plugin (netdata.plugin_chartsd_cpufreq)
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[netdata.plugin_chartsd_example]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_chartsd_example
	# chart type = area
	# type = netdata
	# family = charts.d
	# context = netdata.plugin_charts
	# units = milliseconds / run
	# priority = 145000
	# gap when lost iterations above = 1
	# name = netdata.plugin_chartsd_example
	# title = Execution time for example plugin (netdata.plugin_chartsd_example)
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[netdata.plugin_chartsd_sensors]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_chartsd_sensors
	# chart type = area
	# type = netdata
	# family = charts.d
	# context = netdata.plugin_charts
	# units = milliseconds / run
	# priority = 145000
	# gap when lost iterations above = 1
	# name = netdata.plugin_chartsd_sensors
	# title = Execution time for sensors plugin (netdata.plugin_chartsd_sensors)
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[apps.major_faults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.major_faults
	# chart type = stacked
	# type = apps
	# family = swap
	# context = apps.major_faults
	# units = page faults/s
	# priority = 20010
	# gap when lost iterations above = 1
	# name = apps.major_faults
	# title = Apps Major Page Faults (swap read) (apps.major_faults)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 1
	# dim wifi divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 1
	# dim log divisor = 1
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 1
	# dim cups divisor = 1
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 1
	# dim gnome divisor = 1
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 1
	# dim X divisor = 1

[apps.minor_faults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.minor_faults
	# chart type = stacked
	# type = apps
	# family = mem
	# context = apps.minor_faults
	# units = page faults/s
	# priority = 20011
	# gap when lost iterations above = 1
	# name = apps.minor_faults
	# title = Apps Minor Page Faults (apps.minor_faults)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 1
	# dim wifi divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 1
	# dim log divisor = 1
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 1
	# dim cups divisor = 1
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 1
	# dim gnome divisor = 1
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 1
	# dim X divisor = 1

[apps.lreads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.lreads
	# chart type = stacked
	# type = apps
	# family = disk
	# context = apps.lreads
	# units = kilobytes/s
	# priority = 20042
	# gap when lost iterations above = 1
	# name = apps.lreads
	# title = Apps Disk Logical Reads (apps.lreads)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 1
	# dim other divisor = 1024
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1024
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1024
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 1
	# dim wifi divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 1
	# dim kernel divisor = 1024
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 1
	# dim cron divisor = 1024
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 1
	# dim log divisor = 1024
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 1
	# dim cups divisor = 1024
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 1
	# dim gnome divisor = 1024
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 1
	# dim X divisor = 1024

[apps.lwrites]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.lwrites
	# chart type = stacked
	# type = apps
	# family = disk
	# context = apps.lwrites
	# units = kilobytes/s
	# priority = 20042
	# gap when lost iterations above = 1
	# name = apps.lwrites
	# title = Apps I/O Logical Writes (apps.lwrites)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 1
	# dim other divisor = 1024
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1024
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1024
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 1
	# dim wifi divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 1
	# dim kernel divisor = 1024
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 1
	# dim cron divisor = 1024
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 1
	# dim log divisor = 1024
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 1
	# dim cups divisor = 1024
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 1
	# dim gnome divisor = 1024
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 1
	# dim X divisor = 1024

[system.softirqs]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.softirqs
	# chart type = stacked
	# type = system
	# family = softirqs
	# context = system.softirqs
	# units = softirqs/s
	# priority = 950
	# gap when lost iterations above = 1
	# name = system.softirqs
	# title = System softirqs (system.softirqs)
	# dim HI name = HI
	# dim HI algorithm = incremental
	# dim HI multiplier = 1
	# dim HI divisor = 1
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim BLOCK_IOPOLL name = BLOCK_IOPOLL
	# dim BLOCK_IOPOLL algorithm = incremental
	# dim BLOCK_IOPOLL multiplier = 1
	# dim BLOCK_IOPOLL divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim HRTIMER name = HRTIMER
	# dim HRTIMER algorithm = incremental
	# dim HRTIMER multiplier = 1
	# dim HRTIMER divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu0_softirqs]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu0_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# context = cpu.softirqs
	# units = softirqs/s
	# priority = 3000
	# gap when lost iterations above = 1
	# name = cpu.cpu0_softirqs
	# title = CPU0 softirqs (cpu.cpu0_softirqs)
	# dim HI name = HI
	# dim HI algorithm = incremental
	# dim HI multiplier = 1
	# dim HI divisor = 1
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim BLOCK_IOPOLL name = BLOCK_IOPOLL
	# dim BLOCK_IOPOLL algorithm = incremental
	# dim BLOCK_IOPOLL multiplier = 1
	# dim BLOCK_IOPOLL divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim HRTIMER name = HRTIMER
	# dim HRTIMER algorithm = incremental
	# dim HRTIMER multiplier = 1
	# dim HRTIMER divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.preads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.preads
	# chart type = stacked
	# type = apps
	# family = disk
	# context = apps.preads
	# units = kilobytes/s
	# priority = 20002
	# gap when lost iterations above = 1
	# name = apps.preads
	# title = Apps Disk Reads (apps.preads)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 1
	# dim other divisor = 1024
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1024
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1024
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 1
	# dim wifi divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 1
	# dim kernel divisor = 1024
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 1
	# dim cron divisor = 1024
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 1
	# dim log divisor = 1024
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 1
	# dim cups divisor = 1024
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 1
	# dim gnome divisor = 1024
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 1
	# dim X divisor = 1024

[cpu.cpu1_softirqs]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu1_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# context = cpu.softirqs
	# units = softirqs/s
	# priority = 3001
	# gap when lost iterations above = 1
	# name = cpu.cpu1_softirqs
	# title = CPU1 softirqs (cpu.cpu1_softirqs)
	# dim HI name = HI
	# dim HI algorithm = incremental
	# dim HI multiplier = 1
	# dim HI divisor = 1
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim BLOCK_IOPOLL name = BLOCK_IOPOLL
	# dim BLOCK_IOPOLL algorithm = incremental
	# dim BLOCK_IOPOLL multiplier = 1
	# dim BLOCK_IOPOLL divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim HRTIMER name = HRTIMER
	# dim HRTIMER algorithm = incremental
	# dim HRTIMER multiplier = 1
	# dim HRTIMER divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.pwrites]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.pwrites
	# chart type = stacked
	# type = apps
	# family = disk
	# context = apps.pwrites
	# units = kilobytes/s
	# priority = 20002
	# gap when lost iterations above = 1
	# name = apps.pwrites
	# title = Apps Disk Writes (apps.pwrites)
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 1
	# dim other divisor = 1024
	# dim ksmd name = ksmd
	# dim ksmd algorithm = incremental
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1024
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1024
	# dim wifi name = wifi
	# dim wifi algorithm = incremental
	# dim wifi multiplier = 1
	# dim wifi divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim kernel name = kernel
	# dim kernel algorithm = incremental
	# dim kernel multiplier = 1
	# dim kernel divisor = 1024
	# dim cron name = cron
	# dim cron algorithm = incremental
	# dim cron multiplier = 1
	# dim cron divisor = 1024
	# dim log name = log
	# dim log algorithm = incremental
	# dim log multiplier = 1
	# dim log divisor = 1024
	# dim cups name = cups
	# dim cups algorithm = incremental
	# dim cups multiplier = 1
	# dim cups divisor = 1024
	# dim gnome name = gnome
	# dim gnome algorithm = incremental
	# dim gnome multiplier = 1
	# dim gnome divisor = 1024
	# dim X name = X
	# dim X algorithm = incremental
	# dim X multiplier = 1
	# dim X divisor = 1024

[cpu.cpu2_softirqs]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu2_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# context = cpu.softirqs
	# units = softirqs/s
	# priority = 3002
	# gap when lost iterations above = 1
	# name = cpu.cpu2_softirqs
	# title = CPU2 softirqs (cpu.cpu2_softirqs)
	# dim HI name = HI
	# dim HI algorithm = incremental
	# dim HI multiplier = 1
	# dim HI divisor = 1
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim BLOCK_IOPOLL name = BLOCK_IOPOLL
	# dim BLOCK_IOPOLL algorithm = incremental
	# dim BLOCK_IOPOLL multiplier = 1
	# dim BLOCK_IOPOLL divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim HRTIMER name = HRTIMER
	# dim HRTIMER algorithm = incremental
	# dim HRTIMER multiplier = 1
	# dim HRTIMER divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.files]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.files
	# chart type = stacked
	# type = apps
	# family = disk
	# context = apps.files
	# units = open files
	# priority = 20050
	# gap when lost iterations above = 1
	# name = apps.files
	# title = Apps Open Files (apps.files)
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim wifi name = wifi
	# dim wifi algorithm = absolute
	# dim wifi multiplier = 1
	# dim wifi divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim log name = log
	# dim log algorithm = absolute
	# dim log multiplier = 1
	# dim log divisor = 1
	# dim cups name = cups
	# dim cups algorithm = absolute
	# dim cups multiplier = 1
	# dim cups divisor = 1
	# dim gnome name = gnome
	# dim gnome algorithm = absolute
	# dim gnome multiplier = 1
	# dim gnome divisor = 1
	# dim X name = X
	# dim X algorithm = absolute
	# dim X multiplier = 1
	# dim X divisor = 1

[apps.sockets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.sockets
	# chart type = stacked
	# type = apps
	# family = net
	# context = apps.sockets
	# units = open sockets
	# priority = 20051
	# gap when lost iterations above = 1
	# name = apps.sockets
	# title = Apps Open Sockets (apps.sockets)
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim wifi name = wifi
	# dim wifi algorithm = absolute
	# dim wifi multiplier = 1
	# dim wifi divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim log name = log
	# dim log algorithm = absolute
	# dim log multiplier = 1
	# dim log divisor = 1
	# dim cups name = cups
	# dim cups algorithm = absolute
	# dim cups multiplier = 1
	# dim cups divisor = 1
	# dim gnome name = gnome
	# dim gnome algorithm = absolute
	# dim gnome multiplier = 1
	# dim gnome divisor = 1
	# dim X name = X
	# dim X algorithm = absolute
	# dim X multiplier = 1
	# dim X divisor = 1

[cpu.cpu3_softirqs]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu3_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# context = cpu.softirqs
	# units = softirqs/s
	# priority = 3003
	# gap when lost iterations above = 1
	# name = cpu.cpu3_softirqs
	# title = CPU3 softirqs (cpu.cpu3_softirqs)
	# dim HI name = HI
	# dim HI algorithm = incremental
	# dim HI multiplier = 1
	# dim HI divisor = 1
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim BLOCK_IOPOLL name = BLOCK_IOPOLL
	# dim BLOCK_IOPOLL algorithm = incremental
	# dim BLOCK_IOPOLL multiplier = 1
	# dim BLOCK_IOPOLL divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim HRTIMER name = HRTIMER
	# dim HRTIMER algorithm = incremental
	# dim HRTIMER multiplier = 1
	# dim HRTIMER divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.pipes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.pipes
	# chart type = stacked
	# type = apps
	# family = processes
	# context = apps.pipes
	# units = open pipes
	# priority = 20053
	# gap when lost iterations above = 1
	# name = apps.pipes
	# title = Apps Pipes (apps.pipes)
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim wifi name = wifi
	# dim wifi algorithm = absolute
	# dim wifi multiplier = 1
	# dim wifi divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim log name = log
	# dim log algorithm = absolute
	# dim log multiplier = 1
	# dim log divisor = 1
	# dim cups name = cups
	# dim cups algorithm = absolute
	# dim cups multiplier = 1
	# dim cups divisor = 1
	# dim gnome name = gnome
	# dim gnome algorithm = absolute
	# dim gnome multiplier = 1
	# dim gnome divisor = 1
	# dim X name = X
	# dim X algorithm = absolute
	# dim X multiplier = 1
	# dim X divisor = 1

[users.cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.cpu
	# chart type = stacked
	# type = users
	# family = cpu
	# context = users.cpu
	# units = cpu time %
	# priority = 20001
	# gap when lost iterations above = 1
	# name = users.cpu
	# title = Users CPU Time (400% = 4 cores) (users.cpu)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 100
	# dim rtkit divisor = 100
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 100
	# dim libstoragemgmt divisor = 100
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 100
	# dim dbus divisor = 100
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 100
	# dim avahi divisor = 100
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 100
	# dim chrony divisor = 100
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 100
	# dim polkitd divisor = 100
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 100
	# dim nobody divisor = 100
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 100
	# dim gdm divisor = 100
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 100
	# dim colord divisor = 100
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 100
	# dim setroubleshoot divisor = 100
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 100
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 100
	# dim root divisor = 100
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 100
	# dim mosquito divisor = 100

[system.entropy]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.entropy
	# chart type = line
	# type = system
	# family = entropy
	# context = system.entropy
	# units = entropy
	# priority = 1000
	# gap when lost iterations above = 1
	# name = system.entropy
	# title = Available Entropy (system.entropy)
	# dim entropy name = entropy
	# dim entropy algorithm = absolute
	# dim entropy multiplier = 1
	# dim entropy divisor = 1

[net.br0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/net.br0
	# chart type = area
	# type = net
	# family = br0
	# context = net.net
	# units = kilobits/s
	# priority = 7000
	# gap when lost iterations above = 1
	# name = net.br0
	# title = Bandwidth (net.br0)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1024
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1024

[net_packets.br0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.br0
	# chart type = line
	# type = net_packets
	# family = br0
	# context = net.packets
	# units = packets/s
	# priority = 7001
	# gap when lost iterations above = 1
	# name = net_packets.br0
	# title = Packets (net_packets.br0)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net.enp3s0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/net.enp3s0
	# chart type = area
	# type = net
	# family = enp3s0
	# context = net.net
	# units = kilobits/s
	# priority = 7000
	# gap when lost iterations above = 1
	# name = net.enp3s0
	# title = Bandwidth (net.enp3s0)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1024
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1024

[users.mem]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.mem
	# chart type = stacked
	# type = users
	# family = mem
	# context = users.mem
	# units = MB
	# priority = 20003
	# gap when lost iterations above = 1
	# name = users.mem
	# title = Users Dedicated Memory (w/o shared) (users.mem)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 4096
	# dim rtkit divisor = 1048576
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 4096
	# dim libstoragemgmt divisor = 1048576
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 4096
	# dim dbus divisor = 1048576
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 4096
	# dim avahi divisor = 1048576
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 4096
	# dim chrony divisor = 1048576
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 4096
	# dim polkitd divisor = 1048576
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 4096
	# dim nobody divisor = 1048576
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 4096
	# dim gdm divisor = 1048576
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 4096
	# dim colord divisor = 1048576
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 4096
	# dim setroubleshoot divisor = 1048576
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 4096
	# dim netdata divisor = 1048576
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 4096
	# dim root divisor = 1048576
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 4096
	# dim mosquito divisor = 1048576

[net_packets.enp3s0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.enp3s0
	# chart type = line
	# type = net_packets
	# family = enp3s0
	# context = net.packets
	# units = packets/s
	# priority = 7001
	# gap when lost iterations above = 1
	# name = net_packets.enp3s0
	# title = Packets (net_packets.enp3s0)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[disk.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.sda
	# chart type = area
	# type = disk
	# family = sda
	# context = disk.io
	# units = kilobytes/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = disk.sda
	# title = Disk I/O Bandwidth (disk.sda)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.sda
	# chart type = line
	# type = disk_ops
	# family = sda
	# context = disk.ops
	# units = operations/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = disk_ops.sda
	# title = Disk Completed I/O Operations (disk_ops.sda)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_qops.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.sda
	# chart type = line
	# type = disk_qops
	# family = sda
	# context = disk.qops
	# units = operations
	# priority = 2002
	# gap when lost iterations above = 1
	# name = disk_qops.sda
	# title = Disk Current I/O Operations (disk_qops.sda)
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[users.threads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.threads
	# chart type = stacked
	# type = users
	# family = processes
	# context = users.threads
	# units = threads
	# priority = 20005
	# gap when lost iterations above = 1
	# name = users.threads
	# title = Users Threads (users.threads)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1

[disk_backlog.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.sda
	# chart type = area
	# type = disk_backlog
	# family = sda
	# context = disk.backlog
	# units = backlog (ms)
	# priority = 2003
	# gap when lost iterations above = 1
	# name = disk_backlog.sda
	# title = Disk Backlog (disk_backlog.sda)
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.sda
	# chart type = area
	# type = disk_util
	# family = sda
	# context = disk.util
	# units = % of time working
	# priority = 2004
	# gap when lost iterations above = 1
	# name = disk_util.sda
	# title = Disk Utilization Time (disk_util.sda)
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_mops.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_mops.sda
	# chart type = line
	# type = disk_mops
	# family = sda
	# context = disk.mops
	# units = merged operations/s
	# priority = 2021
	# gap when lost iterations above = 1
	# name = disk_mops.sda
	# title = Disk Merged Operations (disk_mops.sda)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[users.processes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.processes
	# chart type = stacked
	# type = users
	# family = processes
	# context = users.processes
	# units = processes
	# priority = 20004
	# gap when lost iterations above = 1
	# name = users.processes
	# title = Users Processes (users.processes)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1

[disk_iotime.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.sda
	# chart type = line
	# type = disk_iotime
	# family = sda
	# context = disk.iotime
	# units = milliseconds/s
	# priority = 2022
	# gap when lost iterations above = 1
	# name = disk_iotime.sda
	# title = Disk Total I/O Time (disk_iotime.sda)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.dm_0
	# chart type = area
	# type = disk
	# family = dm-0
	# context = disk.io
	# units = kilobytes/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = disk.dm_0
	# title = Disk I/O Bandwidth (disk.dm_0)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.dm_0
	# chart type = line
	# type = disk_ops
	# family = dm-0
	# context = disk.ops
	# units = operations/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = disk_ops.dm_0
	# title = Disk Completed I/O Operations (disk_ops.dm_0)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[users.cpu_user]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.cpu_user
	# chart type = stacked
	# type = users
	# family = cpu
	# context = users.cpu_user
	# units = cpu time %
	# priority = 20020
	# gap when lost iterations above = 1
	# name = users.cpu_user
	# title = Users CPU User Time (400% = 4 cores) (users.cpu_user)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 100
	# dim rtkit divisor = 400
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 100
	# dim libstoragemgmt divisor = 400
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 100
	# dim dbus divisor = 400
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 100
	# dim avahi divisor = 400
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 100
	# dim chrony divisor = 400
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 100
	# dim polkitd divisor = 400
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 100
	# dim nobody divisor = 400
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 100
	# dim gdm divisor = 400
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 100
	# dim colord divisor = 400
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 100
	# dim setroubleshoot divisor = 400
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 400
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 100
	# dim root divisor = 400
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 100
	# dim mosquito divisor = 400

[disk_qops.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.dm_0
	# chart type = line
	# type = disk_qops
	# family = dm-0
	# context = disk.qops
	# units = operations
	# priority = 2002
	# gap when lost iterations above = 1
	# name = disk_qops.dm_0
	# title = Disk Current I/O Operations (disk_qops.dm_0)
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[disk_backlog.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.dm_0
	# chart type = area
	# type = disk_backlog
	# family = dm-0
	# context = disk.backlog
	# units = backlog (ms)
	# priority = 2003
	# gap when lost iterations above = 1
	# name = disk_backlog.dm_0
	# title = Disk Backlog (disk_backlog.dm_0)
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.dm_0
	# chart type = area
	# type = disk_util
	# family = dm-0
	# context = disk.util
	# units = % of time working
	# priority = 2004
	# gap when lost iterations above = 1
	# name = disk_util.dm_0
	# title = Disk Utilization Time (disk_util.dm_0)
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_iotime.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.dm_0
	# chart type = line
	# type = disk_iotime
	# family = dm-0
	# context = disk.iotime
	# units = milliseconds/s
	# priority = 2022
	# gap when lost iterations above = 1
	# name = disk_iotime.dm_0
	# title = Disk Total I/O Time (disk_iotime.dm_0)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[users.cpu_system]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.cpu_system
	# chart type = stacked
	# type = users
	# family = cpu
	# context = users.cpu_system
	# units = cpu time %
	# priority = 20021
	# gap when lost iterations above = 1
	# name = users.cpu_system
	# title = Users CPU System Time (400% = 4 cores) (users.cpu_system)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 100
	# dim rtkit divisor = 400
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 100
	# dim libstoragemgmt divisor = 400
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 100
	# dim dbus divisor = 400
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 100
	# dim avahi divisor = 400
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 100
	# dim chrony divisor = 400
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 100
	# dim polkitd divisor = 400
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 100
	# dim nobody divisor = 400
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 100
	# dim gdm divisor = 400
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 100
	# dim colord divisor = 400
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 100
	# dim setroubleshoot divisor = 400
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 400
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 100
	# dim root divisor = 400
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 100
	# dim mosquito divisor = 400

[disk.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.dm_1
	# chart type = area
	# type = disk
	# family = /
	# context = disk.io
	# units = kilobytes/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = disk.dm_1
	# title = Disk I/O Bandwidth (disk.dm_1)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.dm_1
	# chart type = line
	# type = disk_ops
	# family = /
	# context = disk.ops
	# units = operations/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = disk_ops.dm_1
	# title = Disk Completed I/O Operations (disk_ops.dm_1)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_qops.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.dm_1
	# chart type = line
	# type = disk_qops
	# family = /
	# context = disk.qops
	# units = operations
	# priority = 2002
	# gap when lost iterations above = 1
	# name = disk_qops.dm_1
	# title = Disk Current I/O Operations (disk_qops.dm_1)
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[disk_backlog.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.dm_1
	# chart type = area
	# type = disk_backlog
	# family = /
	# context = disk.backlog
	# units = backlog (ms)
	# priority = 2003
	# gap when lost iterations above = 1
	# name = disk_backlog.dm_1
	# title = Disk Backlog (disk_backlog.dm_1)
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.dm_1
	# chart type = area
	# type = disk_util
	# family = /
	# context = disk.util
	# units = % of time working
	# priority = 2004
	# gap when lost iterations above = 1
	# name = disk_util.dm_1
	# title = Disk Utilization Time (disk_util.dm_1)
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_iotime.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.dm_1
	# chart type = line
	# type = disk_iotime
	# family = /
	# context = disk.iotime
	# units = milliseconds/s
	# priority = 2022
	# gap when lost iterations above = 1
	# name = disk_iotime.dm_1
	# title = Disk Total I/O Time (disk_iotime.dm_1)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[users.major_faults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.major_faults
	# chart type = stacked
	# type = users
	# family = swap
	# context = users.major_faults
	# units = page faults/s
	# priority = 20010
	# gap when lost iterations above = 1
	# name = users.major_faults
	# title = Users Major Page Faults (swap read) (users.major_faults)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1

[disk.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.dm_2
	# chart type = area
	# type = disk
	# family = /home
	# context = disk.io
	# units = kilobytes/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = disk.dm_2
	# title = Disk I/O Bandwidth (disk.dm_2)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.dm_2
	# chart type = line
	# type = disk_ops
	# family = /home
	# context = disk.ops
	# units = operations/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = disk_ops.dm_2
	# title = Disk Completed I/O Operations (disk_ops.dm_2)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_qops.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.dm_2
	# chart type = line
	# type = disk_qops
	# family = /home
	# context = disk.qops
	# units = operations
	# priority = 2002
	# gap when lost iterations above = 1
	# name = disk_qops.dm_2
	# title = Disk Current I/O Operations (disk_qops.dm_2)
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[disk_backlog.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.dm_2
	# chart type = area
	# type = disk_backlog
	# family = /home
	# context = disk.backlog
	# units = backlog (ms)
	# priority = 2003
	# gap when lost iterations above = 1
	# name = disk_backlog.dm_2
	# title = Disk Backlog (disk_backlog.dm_2)
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.dm_2
	# chart type = area
	# type = disk_util
	# family = /home
	# context = disk.util
	# units = % of time working
	# priority = 2004
	# gap when lost iterations above = 1
	# name = disk_util.dm_2
	# title = Disk Utilization Time (disk_util.dm_2)
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_iotime.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.dm_2
	# chart type = line
	# type = disk_iotime
	# family = /home
	# context = disk.iotime
	# units = milliseconds/s
	# priority = 2022
	# gap when lost iterations above = 1
	# name = disk_iotime.dm_2
	# title = Disk Total I/O Time (disk_iotime.dm_2)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[users.minor_faults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.minor_faults
	# chart type = stacked
	# type = users
	# family = mem
	# context = users.minor_faults
	# units = page faults/s
	# priority = 20011
	# gap when lost iterations above = 1
	# name = users.minor_faults
	# title = Users Minor Page Faults (users.minor_faults)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1

[disk.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.dm_3
	# chart type = area
	# type = disk
	# family = dm-3
	# context = disk.io
	# units = kilobytes/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = disk.dm_3
	# title = Disk I/O Bandwidth (disk.dm_3)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.dm_3
	# chart type = line
	# type = disk_ops
	# family = dm-3
	# context = disk.ops
	# units = operations/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = disk_ops.dm_3
	# title = Disk Completed I/O Operations (disk_ops.dm_3)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_qops.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.dm_3
	# chart type = line
	# type = disk_qops
	# family = dm-3
	# context = disk.qops
	# units = operations
	# priority = 2002
	# gap when lost iterations above = 1
	# name = disk_qops.dm_3
	# title = Disk Current I/O Operations (disk_qops.dm_3)
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[users.lreads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.lreads
	# chart type = stacked
	# type = users
	# family = disk
	# context = users.lreads
	# units = kilobytes/s
	# priority = 20042
	# gap when lost iterations above = 1
	# name = users.lreads
	# title = Users Disk Logical Reads (users.lreads)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 1
	# dim nobody divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024

[users.lwrites]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.lwrites
	# chart type = stacked
	# type = users
	# family = disk
	# context = users.lwrites
	# units = kilobytes/s
	# priority = 20042
	# gap when lost iterations above = 1
	# name = users.lwrites
	# title = Users I/O Logical Writes (users.lwrites)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 1
	# dim nobody divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024

[disk_backlog.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.dm_3
	# chart type = area
	# type = disk_backlog
	# family = dm-3
	# context = disk.backlog
	# units = backlog (ms)
	# priority = 2003
	# gap when lost iterations above = 1
	# name = disk_backlog.dm_3
	# title = Disk Backlog (disk_backlog.dm_3)
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.dm_3
	# chart type = area
	# type = disk_util
	# family = dm-3
	# context = disk.util
	# units = % of time working
	# priority = 2004
	# gap when lost iterations above = 1
	# name = disk_util.dm_3
	# title = Disk Utilization Time (disk_util.dm_3)
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[users.preads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.preads
	# chart type = stacked
	# type = users
	# family = disk
	# context = users.preads
	# units = kilobytes/s
	# priority = 20002
	# gap when lost iterations above = 1
	# name = users.preads
	# title = Users Disk Reads (users.preads)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 1
	# dim nobody divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024

[disk_iotime.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.dm_3
	# chart type = line
	# type = disk_iotime
	# family = dm-3
	# context = disk.iotime
	# units = milliseconds/s
	# priority = 2022
	# gap when lost iterations above = 1
	# name = disk_iotime.dm_3
	# title = Disk Total I/O Time (disk_iotime.dm_3)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.dm_4
	# chart type = area
	# type = disk
	# family = dm-4
	# context = disk.io
	# units = kilobytes/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = disk.dm_4
	# title = Disk I/O Bandwidth (disk.dm_4)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.dm_4
	# chart type = line
	# type = disk_ops
	# family = dm-4
	# context = disk.ops
	# units = operations/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = disk_ops.dm_4
	# title = Disk Completed I/O Operations (disk_ops.dm_4)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_qops.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.dm_4
	# chart type = line
	# type = disk_qops
	# family = dm-4
	# context = disk.qops
	# units = operations
	# priority = 2002
	# gap when lost iterations above = 1
	# name = disk_qops.dm_4
	# title = Disk Current I/O Operations (disk_qops.dm_4)
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[users.pwrites]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.pwrites
	# chart type = stacked
	# type = users
	# family = disk
	# context = users.pwrites
	# units = kilobytes/s
	# priority = 20002
	# gap when lost iterations above = 1
	# name = users.pwrites
	# title = Users Disk Writes (users.pwrites)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim nobody name = nobody
	# dim nobody algorithm = incremental
	# dim nobody multiplier = 1
	# dim nobody divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024

[disk_backlog.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.dm_4
	# chart type = area
	# type = disk_backlog
	# family = dm-4
	# context = disk.backlog
	# units = backlog (ms)
	# priority = 2003
	# gap when lost iterations above = 1
	# name = disk_backlog.dm_4
	# title = Disk Backlog (disk_backlog.dm_4)
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.dm_4
	# chart type = area
	# type = disk_util
	# family = dm-4
	# context = disk.util
	# units = % of time working
	# priority = 2004
	# gap when lost iterations above = 1
	# name = disk_util.dm_4
	# title = Disk Utilization Time (disk_util.dm_4)
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_iotime.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.dm_4
	# chart type = line
	# type = disk_iotime
	# family = dm-4
	# context = disk.iotime
	# units = milliseconds/s
	# priority = 2022
	# gap when lost iterations above = 1
	# name = disk_iotime.dm_4
	# title = Disk Total I/O Time (disk_iotime.dm_4)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.dm_5
	# chart type = area
	# type = disk
	# family = dm-5
	# context = disk.io
	# units = kilobytes/s
	# priority = 2000
	# gap when lost iterations above = 1
	# name = disk.dm_5
	# title = Disk I/O Bandwidth (disk.dm_5)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.dm_5
	# chart type = line
	# type = disk_ops
	# family = dm-5
	# context = disk.ops
	# units = operations/s
	# priority = 2001
	# gap when lost iterations above = 1
	# name = disk_ops.dm_5
	# title = Disk Completed I/O Operations (disk_ops.dm_5)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[users.files]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.files
	# chart type = stacked
	# type = users
	# family = disk
	# context = users.files
	# units = open files
	# priority = 20050
	# gap when lost iterations above = 1
	# name = users.files
	# title = Users Open Files (users.files)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1

[disk_qops.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.dm_5
	# chart type = line
	# type = disk_qops
	# family = dm-5
	# context = disk.qops
	# units = operations
	# priority = 2002
	# gap when lost iterations above = 1
	# name = disk_qops.dm_5
	# title = Disk Current I/O Operations (disk_qops.dm_5)
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[disk_backlog.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.dm_5
	# chart type = area
	# type = disk_backlog
	# family = dm-5
	# context = disk.backlog
	# units = backlog (ms)
	# priority = 2003
	# gap when lost iterations above = 1
	# name = disk_backlog.dm_5
	# title = Disk Backlog (disk_backlog.dm_5)
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.dm_5
	# chart type = area
	# type = disk_util
	# family = dm-5
	# context = disk.util
	# units = % of time working
	# priority = 2004
	# gap when lost iterations above = 1
	# name = disk_util.dm_5
	# title = Disk Utilization Time (disk_util.dm_5)
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_iotime.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.dm_5
	# chart type = line
	# type = disk_iotime
	# family = dm-5
	# context = disk.iotime
	# units = milliseconds/s
	# priority = 2022
	# gap when lost iterations above = 1
	# name = disk_iotime.dm_5
	# title = Disk Total I/O Time (disk_iotime.dm_5)
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[ipv4.packets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.packets
	# chart type = line
	# type = ipv4
	# family = packets
	# context = ipv4.packets
	# units = packets/s
	# priority = 3000
	# gap when lost iterations above = 1
	# name = ipv4.packets
	# title = IPv4 Packets (ipv4.packets)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim forwarded name = forwarded
	# dim forwarded algorithm = incremental
	# dim forwarded multiplier = 1
	# dim forwarded divisor = 1

[ipv4.fragsout]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.fragsout
	# chart type = line
	# type = ipv4
	# family = fragments
	# context = ipv4.fragsout
	# units = packets/s
	# priority = 3010
	# gap when lost iterations above = 1
	# name = ipv4.fragsout
	# title = IPv4 Fragments Sent (ipv4.fragsout)
	# dim ok name = ok
	# dim ok algorithm = incremental
	# dim ok multiplier = 1
	# dim ok divisor = 1
	# dim failed name = failed
	# dim failed algorithm = incremental
	# dim failed multiplier = -1
	# dim failed divisor = 1
	# dim all name = all
	# dim all algorithm = incremental
	# dim all multiplier = 1
	# dim all divisor = 1

[ipv4.fragsin]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.fragsin
	# chart type = line
	# type = ipv4
	# family = fragments
	# context = ipv4.fragsin
	# units = packets/s
	# priority = 3011
	# gap when lost iterations above = 1
	# name = ipv4.fragsin
	# title = IPv4 Fragments Reassembly (ipv4.fragsin)
	# dim ok name = ok
	# dim ok algorithm = incremental
	# dim ok multiplier = 1
	# dim ok divisor = 1
	# dim failed name = failed
	# dim failed algorithm = incremental
	# dim failed multiplier = -1
	# dim failed divisor = 1
	# dim all name = all
	# dim all algorithm = incremental
	# dim all multiplier = 1
	# dim all divisor = 1

[ipv4.errors]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.errors
	# chart type = line
	# type = ipv4
	# family = errors
	# context = ipv4.errors
	# units = packets/s
	# priority = 3002
	# gap when lost iterations above = 1
	# name = ipv4.errors
	# title = IPv4 Errors (ipv4.errors)
	# dim InDiscards name = InDiscards
	# dim InDiscards algorithm = incremental
	# dim InDiscards multiplier = 1
	# dim InDiscards divisor = 1
	# dim OutDiscards name = OutDiscards
	# dim OutDiscards algorithm = incremental
	# dim OutDiscards multiplier = -1
	# dim OutDiscards divisor = 1
	# dim InHdrErrors name = InHdrErrors
	# dim InHdrErrors algorithm = incremental
	# dim InHdrErrors multiplier = 1
	# dim InHdrErrors divisor = 1
	# dim InAddrErrors name = InAddrErrors
	# dim InAddrErrors algorithm = incremental
	# dim InAddrErrors multiplier = 1
	# dim InAddrErrors divisor = 1
	# dim InUnknownProtos name = InUnknownProtos
	# dim InUnknownProtos algorithm = incremental
	# dim InUnknownProtos multiplier = 1
	# dim InUnknownProtos divisor = 1
	# dim OutNoRoutes name = OutNoRoutes
	# dim OutNoRoutes algorithm = incremental
	# dim OutNoRoutes multiplier = -1
	# dim OutNoRoutes divisor = 1

[ipv4.tcpsock]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcpsock
	# chart type = line
	# type = ipv4
	# family = tcp
	# context = ipv4.tcpsock
	# units = active connections
	# priority = 2500
	# gap when lost iterations above = 1
	# name = ipv4.tcpsock
	# title = IPv4 TCP Connections (ipv4.tcpsock)
	# dim connections name = connections
	# dim connections algorithm = absolute
	# dim connections multiplier = 1
	# dim connections divisor = 1

[users.sockets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.sockets
	# chart type = stacked
	# type = users
	# family = net
	# context = users.sockets
	# units = open sockets
	# priority = 20051
	# gap when lost iterations above = 1
	# name = users.sockets
	# title = Users Open Sockets (users.sockets)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1

[ipv4.tcppackets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcppackets
	# chart type = line
	# type = ipv4
	# family = tcp
	# context = ipv4.tcppackets
	# units = packets/s
	# priority = 2600
	# gap when lost iterations above = 1
	# name = ipv4.tcppackets
	# title = IPv4 TCP Packets (ipv4.tcppackets)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[ipv4.tcperrors]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcperrors
	# chart type = line
	# type = ipv4
	# family = tcp
	# context = ipv4.tcperrors
	# units = packets/s
	# priority = 2700
	# gap when lost iterations above = 1
	# name = ipv4.tcperrors
	# title = IPv4 TCP Errors (ipv4.tcperrors)
	# dim InErrs name = InErrs
	# dim InErrs algorithm = incremental
	# dim InErrs multiplier = 1
	# dim InErrs divisor = 1
	# dim RetransSegs name = RetransSegs
	# dim RetransSegs algorithm = incremental
	# dim RetransSegs multiplier = -1
	# dim RetransSegs divisor = 1

[ipv4.tcphandshake]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcphandshake
	# chart type = line
	# type = ipv4
	# family = tcp
	# context = ipv4.tcphandshake
	# units = events/s
	# priority = 2900
	# gap when lost iterations above = 1
	# name = ipv4.tcphandshake
	# title = IPv4 TCP Handshake Issues (ipv4.tcphandshake)
	# dim EstabResets name = EstabResets
	# dim EstabResets algorithm = incremental
	# dim EstabResets multiplier = 1
	# dim EstabResets divisor = 1
	# dim OutRsts name = OutRsts
	# dim OutRsts algorithm = incremental
	# dim OutRsts multiplier = -1
	# dim OutRsts divisor = 1
	# dim ActiveOpens name = ActiveOpens
	# dim ActiveOpens algorithm = incremental
	# dim ActiveOpens multiplier = 1
	# dim ActiveOpens divisor = 1
	# dim PassiveOpens name = PassiveOpens
	# dim PassiveOpens algorithm = incremental
	# dim PassiveOpens multiplier = 1
	# dim PassiveOpens divisor = 1
	# dim AttemptFails name = AttemptFails
	# dim AttemptFails algorithm = incremental
	# dim AttemptFails multiplier = 1
	# dim AttemptFails divisor = 1

[users.pipes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/users.pipes
	# chart type = stacked
	# type = users
	# family = processes
	# context = users.pipes
	# units = open pipes
	# priority = 20053
	# gap when lost iterations above = 1
	# name = users.pipes
	# title = Users Pipes (users.pipes)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1

[ipv4.udppackets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.udppackets
	# chart type = line
	# type = ipv4
	# family = udp
	# context = ipv4.udppackets
	# units = packets/s
	# priority = 2601
	# gap when lost iterations above = 1
	# name = ipv4.udppackets
	# title = IPv4 UDP Packets (ipv4.udppackets)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[ipv4.udperrors]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.udperrors
	# chart type = line
	# type = ipv4
	# family = udp
	# context = ipv4.udperrors
	# units = events/s
	# priority = 2701
	# gap when lost iterations above = 1
	# name = ipv4.udperrors
	# title = IPv4 UDP Errors (ipv4.udperrors)
	# dim RcvbufErrors name = RcvbufErrors
	# dim RcvbufErrors algorithm = incremental
	# dim RcvbufErrors multiplier = 1
	# dim RcvbufErrors divisor = 1
	# dim SndbufErrors name = SndbufErrors
	# dim SndbufErrors algorithm = incremental
	# dim SndbufErrors multiplier = -1
	# dim SndbufErrors divisor = 1
	# dim InErrors name = InErrors
	# dim InErrors algorithm = incremental
	# dim InErrors multiplier = 1
	# dim InErrors divisor = 1
	# dim NoPorts name = NoPorts
	# dim NoPorts algorithm = incremental
	# dim NoPorts multiplier = 1
	# dim NoPorts divisor = 1

[groups.cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.cpu
	# chart type = stacked
	# type = groups
	# family = cpu
	# context = groups.cpu
	# units = cpu time %
	# priority = 20001
	# gap when lost iterations above = 1
	# name = groups.cpu
	# title = User Groups CPU Time (400% = 4 cores) (groups.cpu)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 100
	# dim rtkit divisor = 100
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 100
	# dim libstoragemgmt divisor = 100
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 100
	# dim dbus divisor = 100
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 100
	# dim avahi divisor = 100
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 100
	# dim chrony divisor = 100
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 100
	# dim polkitd divisor = 100
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 100
	# dim dip divisor = 100
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 100
	# dim gdm divisor = 100
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 100
	# dim colord divisor = 100
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 100
	# dim setroubleshoot divisor = 100
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 100
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 100
	# dim root divisor = 100
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 100
	# dim mosquito divisor = 100
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 100
	# dim slocate divisor = 100

[system.ipv6]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ipv6
	# chart type = area
	# type = system
	# family = network
	# context = system.ipv6
	# units = kilobits/s
	# priority = 500
	# gap when lost iterations above = 1
	# name = system.ipv6
	# title = IPv6 Bandwidth (system.ipv6)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1024
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1024

[ipv6.packets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.packets
	# chart type = line
	# type = ipv6
	# family = packets
	# context = ipv6.packets
	# units = packets/s
	# priority = 3000
	# gap when lost iterations above = 1
	# name = ipv6.packets
	# title = IPv6 Packets (ipv6.packets)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim forwarded name = forwarded
	# dim forwarded algorithm = incremental
	# dim forwarded multiplier = 1
	# dim forwarded divisor = 1
	# dim delivers name = delivers
	# dim delivers algorithm = incremental
	# dim delivers multiplier = -1
	# dim delivers divisor = 1

[ipv6.udppackets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.udppackets
	# chart type = line
	# type = ipv6
	# family = udp
	# context = ipv6.udppackets
	# units = packets/s
	# priority = 3601
	# gap when lost iterations above = 1
	# name = ipv6.udppackets
	# title = IPv6 UDP Packets (ipv6.udppackets)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[ipv6.mcast]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.mcast
	# chart type = area
	# type = ipv6
	# family = multicast
	# context = ipv6.mcast
	# units = kilobits/s
	# priority = 9000
	# gap when lost iterations above = 1
	# name = ipv6.mcast
	# title = IPv6 Multicast Bandwidth (ipv6.mcast)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1024
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1024

[groups.mem]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.mem
	# chart type = stacked
	# type = groups
	# family = mem
	# context = groups.mem
	# units = MB
	# priority = 20003
	# gap when lost iterations above = 1
	# name = groups.mem
	# title = User Groups Dedicated Memory (w/o shared) (groups.mem)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 4096
	# dim rtkit divisor = 1048576
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 4096
	# dim libstoragemgmt divisor = 1048576
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 4096
	# dim dbus divisor = 1048576
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 4096
	# dim avahi divisor = 1048576
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 4096
	# dim chrony divisor = 1048576
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 4096
	# dim polkitd divisor = 1048576
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 4096
	# dim dip divisor = 1048576
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 4096
	# dim gdm divisor = 1048576
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 4096
	# dim colord divisor = 1048576
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 4096
	# dim setroubleshoot divisor = 1048576
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 4096
	# dim netdata divisor = 1048576
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 4096
	# dim root divisor = 1048576
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 4096
	# dim mosquito divisor = 1048576
	# dim slocate name = slocate
	# dim slocate algorithm = absolute
	# dim slocate multiplier = 4096
	# dim slocate divisor = 1048576

[ipv6.mcastpkts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.mcastpkts
	# chart type = line
	# type = ipv6
	# family = multicast
	# context = ipv6.mcastpkts
	# units = packets/s
	# priority = 9500
	# gap when lost iterations above = 1
	# name = ipv6.mcastpkts
	# title = IPv6 Multicast Packets (ipv6.mcastpkts)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[ipv6.icmp]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmp
	# chart type = line
	# type = ipv6
	# family = icmp
	# context = ipv6.icmp
	# units = messages/s
	# priority = 10000
	# gap when lost iterations above = 1
	# name = ipv6.icmp
	# title = IPv6 ICMP Messages (ipv6.icmp)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[ipv6.icmprouter]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmprouter
	# chart type = line
	# type = ipv6
	# family = icmp
	# context = ipv6.icmprouter
	# units = messages/s
	# priority = 10400
	# gap when lost iterations above = 1
	# name = ipv6.icmprouter
	# title = IPv6 Router Messages (ipv6.icmprouter)
	# dim InSolicits name = InSolicits
	# dim InSolicits algorithm = incremental
	# dim InSolicits multiplier = 1
	# dim InSolicits divisor = 1
	# dim OutSolicits name = OutSolicits
	# dim OutSolicits algorithm = incremental
	# dim OutSolicits multiplier = -1
	# dim OutSolicits divisor = 1
	# dim InAdvertisements name = InAdvertisements
	# dim InAdvertisements algorithm = incremental
	# dim InAdvertisements multiplier = 1
	# dim InAdvertisements divisor = 1
	# dim OutAdvertisements name = OutAdvertisements
	# dim OutAdvertisements algorithm = incremental
	# dim OutAdvertisements multiplier = -1
	# dim OutAdvertisements divisor = 1

[ipv6.icmpneighbor]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmpneighbor
	# chart type = line
	# type = ipv6
	# family = icmp
	# context = ipv6.icmpneighbor
	# units = messages/s
	# priority = 10500
	# gap when lost iterations above = 1
	# name = ipv6.icmpneighbor
	# title = IPv6 Neighbor Messages (ipv6.icmpneighbor)
	# dim InSolicits name = InSolicits
	# dim InSolicits algorithm = incremental
	# dim InSolicits multiplier = 1
	# dim InSolicits divisor = 1
	# dim OutSolicits name = OutSolicits
	# dim OutSolicits algorithm = incremental
	# dim OutSolicits multiplier = -1
	# dim OutSolicits divisor = 1
	# dim InAdvertisements name = InAdvertisements
	# dim InAdvertisements algorithm = incremental
	# dim InAdvertisements multiplier = 1
	# dim InAdvertisements divisor = 1
	# dim OutAdvertisements name = OutAdvertisements
	# dim OutAdvertisements algorithm = incremental
	# dim OutAdvertisements multiplier = -1
	# dim OutAdvertisements divisor = 1

[ipv6.icmpmldv2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmpmldv2
	# chart type = line
	# type = ipv6
	# family = icmp
	# context = ipv6.icmpmldv2
	# units = reports/s
	# priority = 10600
	# gap when lost iterations above = 1
	# name = ipv6.icmpmldv2
	# title = IPv6 ICMP MLDv2 Reports (ipv6.icmpmldv2)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[groups.threads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.threads
	# chart type = stacked
	# type = groups
	# family = processes
	# context = groups.threads
	# units = threads
	# priority = 20005
	# gap when lost iterations above = 1
	# name = groups.threads
	# title = User Groups Threads (groups.threads)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1
	# dim slocate name = slocate
	# dim slocate algorithm = absolute
	# dim slocate multiplier = 1
	# dim slocate divisor = 1

[ipv6.icmptypes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmptypes
	# chart type = line
	# type = ipv6
	# family = icmp
	# context = ipv6.icmptypes
	# units = messages/s
	# priority = 10700
	# gap when lost iterations above = 1
	# name = ipv6.icmptypes
	# title = IPv6 ICMP Types (ipv6.icmptypes)
	# dim InType1 name = InType1
	# dim InType1 algorithm = incremental
	# dim InType1 multiplier = 1
	# dim InType1 divisor = 1
	# dim InType128 name = InType128
	# dim InType128 algorithm = incremental
	# dim InType128 multiplier = 1
	# dim InType128 divisor = 1
	# dim InType129 name = InType129
	# dim InType129 algorithm = incremental
	# dim InType129 multiplier = 1
	# dim InType129 divisor = 1
	# dim InType136 name = InType136
	# dim InType136 algorithm = incremental
	# dim InType136 multiplier = 1
	# dim InType136 divisor = 1
	# dim OutType1 name = OutType1
	# dim OutType1 algorithm = incremental
	# dim OutType1 multiplier = -1
	# dim OutType1 divisor = 1
	# dim OutType128 name = OutType128
	# dim OutType128 algorithm = incremental
	# dim OutType128 multiplier = -1
	# dim OutType128 divisor = 1
	# dim OutType129 name = OutType129
	# dim OutType129 algorithm = incremental
	# dim OutType129 multiplier = -1
	# dim OutType129 divisor = 1
	# dim OutType133 name = OutType133
	# dim OutType133 algorithm = incremental
	# dim OutType133 multiplier = -1
	# dim OutType133 divisor = 1
	# dim OutType135 name = OutType135
	# dim OutType135 algorithm = incremental
	# dim OutType135 multiplier = -1
	# dim OutType135 divisor = 1
	# dim OutType143 name = OutType143
	# dim OutType143 algorithm = incremental
	# dim OutType143 multiplier = -1
	# dim OutType143 divisor = 1

[ipv6.ect]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.ect
	# chart type = line
	# type = ipv6
	# family = packets
	# context = ipv6.ect
	# units = packets/s
	# priority = 10800
	# gap when lost iterations above = 1
	# name = ipv6.ect
	# title = IPv6 ECT Packets (ipv6.ect)
	# dim InNoECTPkts name = InNoECTPkts
	# dim InNoECTPkts algorithm = incremental
	# dim InNoECTPkts multiplier = 1
	# dim InNoECTPkts divisor = 1
	# dim InECT1Pkts name = InECT1Pkts
	# dim InECT1Pkts algorithm = incremental
	# dim InECT1Pkts multiplier = 1
	# dim InECT1Pkts divisor = 1
	# dim InECT0Pkts name = InECT0Pkts
	# dim InECT0Pkts algorithm = incremental
	# dim InECT0Pkts multiplier = 1
	# dim InECT0Pkts divisor = 1
	# dim InCEPkts name = InCEPkts
	# dim InCEPkts algorithm = incremental
	# dim InCEPkts multiplier = 1
	# dim InCEPkts divisor = 1

[system.ipv4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ipv4
	# chart type = area
	# type = system
	# family = network
	# context = system.ipv4
	# units = kilobits/s
	# priority = 500
	# gap when lost iterations above = 1
	# name = system.ipv4
	# title = IPv4 Bandwidth (system.ipv4)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1024
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1024

[groups.processes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.processes
	# chart type = stacked
	# type = groups
	# family = processes
	# context = groups.processes
	# units = processes
	# priority = 20004
	# gap when lost iterations above = 1
	# name = groups.processes
	# title = User Groups Processes (groups.processes)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1
	# dim slocate name = slocate
	# dim slocate algorithm = absolute
	# dim slocate multiplier = 1
	# dim slocate divisor = 1

[groups.cpu_user]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.cpu_user
	# chart type = stacked
	# type = groups
	# family = cpu
	# context = groups.cpu_user
	# units = cpu time %
	# priority = 20020
	# gap when lost iterations above = 1
	# name = groups.cpu_user
	# title = User Groups CPU User Time (400% = 4 cores) (groups.cpu_user)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 100
	# dim rtkit divisor = 400
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 100
	# dim libstoragemgmt divisor = 400
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 100
	# dim dbus divisor = 400
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 100
	# dim avahi divisor = 400
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 100
	# dim chrony divisor = 400
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 100
	# dim polkitd divisor = 400
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 100
	# dim dip divisor = 400
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 100
	# dim gdm divisor = 400
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 100
	# dim colord divisor = 400
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 100
	# dim setroubleshoot divisor = 400
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 400
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 100
	# dim root divisor = 400
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 100
	# dim mosquito divisor = 400
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 100
	# dim slocate divisor = 400

[ipv4.mcast]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.mcast
	# chart type = area
	# type = ipv4
	# family = multicast
	# context = ipv4.mcast
	# units = kilobits/s
	# priority = 9000
	# gap when lost iterations above = 1
	# name = ipv4.mcast
	# title = IPv4 Multicast Bandwidth (ipv4.mcast)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1024
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1024

[ipv4.bcast]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.bcast
	# chart type = area
	# type = ipv4
	# family = broadcast
	# context = ipv4.bcast
	# units = kilobits/s
	# priority = 8000
	# gap when lost iterations above = 1
	# name = ipv4.bcast
	# title = IPv4 Broadcast Bandwidth (ipv4.bcast)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1024
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1024

[ipv4.mcastpkts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.mcastpkts
	# chart type = line
	# type = ipv4
	# family = multicast
	# context = ipv4.mcastpkts
	# units = packets/s
	# priority = 9500
	# gap when lost iterations above = 1
	# name = ipv4.mcastpkts
	# title = IPv4 Multicast Packets (ipv4.mcastpkts)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[groups.cpu_system]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.cpu_system
	# chart type = stacked
	# type = groups
	# family = cpu
	# context = groups.cpu_system
	# units = cpu time %
	# priority = 20021
	# gap when lost iterations above = 1
	# name = groups.cpu_system
	# title = User Groups CPU System Time (400% = 4 cores) (groups.cpu_system)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 100
	# dim rtkit divisor = 400
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 100
	# dim libstoragemgmt divisor = 400
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 100
	# dim dbus divisor = 400
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 100
	# dim avahi divisor = 400
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 100
	# dim chrony divisor = 400
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 100
	# dim polkitd divisor = 400
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 100
	# dim dip divisor = 400
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 100
	# dim gdm divisor = 400
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 100
	# dim colord divisor = 400
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 100
	# dim setroubleshoot divisor = 400
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 100
	# dim netdata divisor = 400
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 100
	# dim root divisor = 400
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 100
	# dim mosquito divisor = 400
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 100
	# dim slocate divisor = 400

[ipv4.bcastpkts]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.bcastpkts
	# chart type = line
	# type = ipv4
	# family = broadcast
	# context = ipv4.bcastpkts
	# units = packets/s
	# priority = 8500
	# gap when lost iterations above = 1
	# name = ipv4.bcastpkts
	# title = IPv4 Broadcast Packets (ipv4.bcastpkts)
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1

[netfilter.conntrack_sockets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_sockets
	# chart type = line
	# type = netfilter
	# family = conntrack
	# context = netfilter.conntrack_sockets
	# units = active connections
	# priority = 1000
	# gap when lost iterations above = 1
	# name = netfilter.conntrack_sockets
	# title = Connection Tracker Connections (netfilter.conntrack_sockets)
	# dim connections name = connections
	# dim connections algorithm = absolute
	# dim connections multiplier = 1
	# dim connections divisor = 1

[netfilter.conntrack_new]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_new
	# chart type = line
	# type = netfilter
	# family = conntrack
	# context = netfilter.conntrack_new
	# units = connections/s
	# priority = 1001
	# gap when lost iterations above = 1
	# name = netfilter.conntrack_new
	# title = Connection Tracker New Connections (netfilter.conntrack_new)
	# dim new name = new
	# dim new algorithm = incremental
	# dim new multiplier = 1
	# dim new divisor = 1
	# dim ignore name = ignore
	# dim ignore algorithm = incremental
	# dim ignore multiplier = -1
	# dim ignore divisor = 1
	# dim invalid name = invalid
	# dim invalid algorithm = incremental
	# dim invalid multiplier = -1
	# dim invalid divisor = 1

[netfilter.conntrack_changes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_changes
	# chart type = line
	# type = netfilter
	# family = conntrack
	# context = netfilter.conntrack_changes
	# units = changes/s
	# priority = 1002
	# gap when lost iterations above = 1
	# name = netfilter.conntrack_changes
	# title = Connection Tracker Changes (netfilter.conntrack_changes)
	# dim inserted name = inserted
	# dim inserted algorithm = incremental
	# dim inserted multiplier = 1
	# dim inserted divisor = 1
	# dim deleted name = deleted
	# dim deleted algorithm = incremental
	# dim deleted multiplier = -1
	# dim deleted divisor = 1
	# dim delete_list name = delete_list
	# dim delete_list algorithm = incremental
	# dim delete_list multiplier = -1
	# dim delete_list divisor = 1

[netfilter.conntrack_expect]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_expect
	# chart type = line
	# type = netfilter
	# family = conntrack
	# context = netfilter.conntrack_expect
	# units = expectations/s
	# priority = 1003
	# gap when lost iterations above = 1
	# name = netfilter.conntrack_expect
	# title = Connection Tracker Expectations (netfilter.conntrack_expect)
	# dim created name = created
	# dim created algorithm = incremental
	# dim created multiplier = 1
	# dim created divisor = 1
	# dim deleted name = deleted
	# dim deleted algorithm = incremental
	# dim deleted multiplier = -1
	# dim deleted divisor = 1
	# dim new name = new
	# dim new algorithm = incremental
	# dim new multiplier = 1
	# dim new divisor = 1

[netfilter.conntrack_search]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_search
	# chart type = line
	# type = netfilter
	# family = conntrack
	# context = netfilter.conntrack_search
	# units = searches/s
	# priority = 1010
	# gap when lost iterations above = 1
	# name = netfilter.conntrack_search
	# title = Connection Tracker Searches (netfilter.conntrack_search)
	# dim searched name = searched
	# dim searched algorithm = incremental
	# dim searched multiplier = 1
	# dim searched divisor = 1
	# dim restarted name = restarted
	# dim restarted algorithm = incremental
	# dim restarted multiplier = -1
	# dim restarted divisor = 1
	# dim found name = found
	# dim found algorithm = incremental
	# dim found multiplier = 1
	# dim found divisor = 1

[netfilter.conntrack_errors]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_errors
	# chart type = line
	# type = netfilter
	# family = conntrack
	# context = netfilter.conntrack_errors
	# units = events/s
	# priority = 1005
	# gap when lost iterations above = 1
	# name = netfilter.conntrack_errors
	# title = Connection Tracker Errors (netfilter.conntrack_errors)
	# dim icmp_error name = icmp_error
	# dim icmp_error algorithm = incremental
	# dim icmp_error multiplier = 1
	# dim icmp_error divisor = 1
	# dim insert_failed name = insert_failed
	# dim insert_failed algorithm = incremental
	# dim insert_failed multiplier = -1
	# dim insert_failed divisor = 1
	# dim drop name = drop
	# dim drop algorithm = incremental
	# dim drop multiplier = -1
	# dim drop divisor = 1
	# dim early_drop name = early_drop
	# dim early_drop algorithm = incremental
	# dim early_drop multiplier = -1
	# dim early_drop divisor = 1

[groups.major_faults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.major_faults
	# chart type = stacked
	# type = groups
	# family = swap
	# context = groups.major_faults
	# units = page faults/s
	# priority = 20010
	# gap when lost iterations above = 1
	# name = groups.major_faults
	# title = User Groups Major Page Faults (swap read) (groups.major_faults)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 1
	# dim slocate divisor = 1

[system.cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.cpu
	# chart type = stacked
	# type = system
	# family = cpu
	# context = system.cpu
	# units = percentage
	# priority = 100
	# gap when lost iterations above = 1
	# name = system.cpu
	# title = Total CPU utilization (system.cpu)
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu0
	# chart type = stacked
	# type = cpu
	# family = utilization
	# context = cpu.cpu
	# units = percentage
	# priority = 1000
	# gap when lost iterations above = 1
	# name = cpu.cpu0
	# title = Core utilization (cpu.cpu0)
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu1
	# chart type = stacked
	# type = cpu
	# family = utilization
	# context = cpu.cpu
	# units = percentage
	# priority = 1000
	# gap when lost iterations above = 1
	# name = cpu.cpu1
	# title = Core utilization (cpu.cpu1)
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[groups.minor_faults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.minor_faults
	# chart type = stacked
	# type = groups
	# family = mem
	# context = groups.minor_faults
	# units = page faults/s
	# priority = 20011
	# gap when lost iterations above = 1
	# name = groups.minor_faults
	# title = User Groups Minor Page Faults (groups.minor_faults)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 1
	# dim slocate divisor = 1

[cpu.cpu2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu2
	# chart type = stacked
	# type = cpu
	# family = utilization
	# context = cpu.cpu
	# units = percentage
	# priority = 1000
	# gap when lost iterations above = 1
	# name = cpu.cpu2
	# title = Core utilization (cpu.cpu2)
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu3
	# chart type = stacked
	# type = cpu
	# family = utilization
	# context = cpu.cpu
	# units = percentage
	# priority = 1000
	# gap when lost iterations above = 1
	# name = cpu.cpu3
	# title = Core utilization (cpu.cpu3)
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[system.intr]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.intr
	# chart type = line
	# type = system
	# family = interrupts
	# context = system.intr
	# units = interrupts/s
	# priority = 900
	# gap when lost iterations above = 1
	# name = system.intr
	# title = CPU Interrupts (system.intr)
	# dim interrupts name = interrupts
	# dim interrupts algorithm = incremental
	# dim interrupts multiplier = 1
	# dim interrupts divisor = 1

[system.ctxt]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ctxt
	# chart type = line
	# type = system
	# family = processes
	# context = system.ctxt
	# units = context switches/s
	# priority = 800
	# gap when lost iterations above = 1
	# name = system.ctxt
	# title = CPU Context Switches (system.ctxt)
	# dim switches name = switches
	# dim switches algorithm = incremental
	# dim switches multiplier = 1
	# dim switches divisor = 1

[system.forks]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.forks
	# chart type = line
	# type = system
	# family = processes
	# context = system.forks
	# units = processes/s
	# priority = 700
	# gap when lost iterations above = 1
	# name = system.forks
	# title = Started Processes (system.forks)
	# dim started name = started
	# dim started algorithm = incremental
	# dim started multiplier = 1
	# dim started divisor = 1

[system.processes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.processes
	# chart type = line
	# type = system
	# family = processes
	# context = system.processes
	# units = processes
	# priority = 600
	# gap when lost iterations above = 1
	# name = system.processes
	# title = System Processes (system.processes)
	# dim running name = running
	# dim running algorithm = absolute
	# dim running multiplier = 1
	# dim running divisor = 1
	# dim blocked name = blocked
	# dim blocked algorithm = absolute
	# dim blocked multiplier = -1
	# dim blocked divisor = 1

[system.ram]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ram
	# chart type = stacked
	# type = system
	# family = ram
	# context = system.ram
	# units = MB
	# priority = 200
	# gap when lost iterations above = 1
	# name = system.ram
	# title = System RAM (system.ram)
	# dim buffers name = buffers
	# dim buffers algorithm = absolute
	# dim buffers multiplier = 1
	# dim buffers divisor = 1024
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1024
	# dim cached name = cached
	# dim cached algorithm = absolute
	# dim cached multiplier = 1
	# dim cached divisor = 1024
	# dim free name = free
	# dim free algorithm = absolute
	# dim free multiplier = 1
	# dim free divisor = 1024

[groups.lreads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.lreads
	# chart type = stacked
	# type = groups
	# family = disk
	# context = groups.lreads
	# units = kilobytes/s
	# priority = 20042
	# gap when lost iterations above = 1
	# name = groups.lreads
	# title = User Groups Disk Logical Reads (groups.lreads)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 1
	# dim dip divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 1
	# dim slocate divisor = 1024

[system.swap]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.swap
	# chart type = stacked
	# type = system
	# family = swap
	# context = system.swap
	# units = MB
	# priority = 201
	# gap when lost iterations above = 1
	# name = system.swap
	# title = System Swap (system.swap)
	# dim free name = free
	# dim free algorithm = absolute
	# dim free multiplier = 1
	# dim free divisor = 1024
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1024

[mem.committed]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.committed
	# chart type = area
	# type = mem
	# family = system
	# context = mem.committed
	# units = MB
	# priority = 5000
	# gap when lost iterations above = 1
	# name = mem.committed
	# title = Committed (Allocated) Memory (mem.committed)
	# dim Committed_AS name = Committed_AS
	# dim Committed_AS algorithm = absolute
	# dim Committed_AS multiplier = 1
	# dim Committed_AS divisor = 1024

[mem.writeback]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.writeback
	# chart type = line
	# type = mem
	# family = kernel
	# context = mem.writeback
	# units = MB
	# priority = 4000
	# gap when lost iterations above = 1
	# name = mem.writeback
	# title = Writeback Memory (mem.writeback)
	# dim Dirty name = Dirty
	# dim Dirty algorithm = absolute
	# dim Dirty multiplier = 1
	# dim Dirty divisor = 1024
	# dim Writeback name = Writeback
	# dim Writeback algorithm = absolute
	# dim Writeback multiplier = 1
	# dim Writeback divisor = 1024
	# dim FuseWriteback name = FuseWriteback
	# dim FuseWriteback algorithm = absolute
	# dim FuseWriteback multiplier = 1
	# dim FuseWriteback divisor = 1024
	# dim NfsWriteback name = NfsWriteback
	# dim NfsWriteback algorithm = absolute
	# dim NfsWriteback multiplier = 1
	# dim NfsWriteback divisor = 1024
	# dim Bounce name = Bounce
	# dim Bounce algorithm = absolute
	# dim Bounce multiplier = 1
	# dim Bounce divisor = 1024

[mem.kernel]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.kernel
	# chart type = stacked
	# type = mem
	# family = kernel
	# context = mem.kernel
	# units = MB
	# priority = 6000
	# gap when lost iterations above = 1
	# name = mem.kernel
	# title = Memory Used by Kernel (mem.kernel)
	# dim Slab name = Slab
	# dim Slab algorithm = absolute
	# dim Slab multiplier = 1
	# dim Slab divisor = 1024
	# dim KernelStack name = KernelStack
	# dim KernelStack algorithm = absolute
	# dim KernelStack multiplier = 1
	# dim KernelStack divisor = 1024
	# dim PageTables name = PageTables
	# dim PageTables algorithm = absolute
	# dim PageTables multiplier = 1
	# dim PageTables divisor = 1024
	# dim VmallocUsed name = VmallocUsed
	# dim VmallocUsed algorithm = absolute
	# dim VmallocUsed multiplier = 1
	# dim VmallocUsed divisor = 1024

[mem.slab]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.slab
	# chart type = stacked
	# type = mem
	# family = slab
	# context = mem.slab
	# units = MB
	# priority = 6500
	# gap when lost iterations above = 1
	# name = mem.slab
	# title = Reclaimable Kernel Memory (mem.slab)
	# dim reclaimable name = reclaimable
	# dim reclaimable algorithm = absolute
	# dim reclaimable multiplier = 1
	# dim reclaimable divisor = 1024
	# dim unreclaimable name = unreclaimable
	# dim unreclaimable algorithm = absolute
	# dim unreclaimable multiplier = 1
	# dim unreclaimable divisor = 1024

[groups.lwrites]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.lwrites
	# chart type = stacked
	# type = groups
	# family = disk
	# context = groups.lwrites
	# units = kilobytes/s
	# priority = 20042
	# gap when lost iterations above = 1
	# name = groups.lwrites
	# title = User Groups I/O Logical Writes (groups.lwrites)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 1
	# dim dip divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 1
	# dim slocate divisor = 1024

[system.swapio]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.swapio
	# chart type = area
	# type = system
	# family = swap
	# context = system.swapio
	# units = kilobytes/s
	# priority = 250
	# gap when lost iterations above = 1
	# name = system.swapio
	# title = Swap I/O (system.swapio)
	# dim in name = in
	# dim in algorithm = incremental
	# dim in multiplier = 4096
	# dim in divisor = 1024
	# dim out name = out
	# dim out algorithm = incremental
	# dim out multiplier = -4096
	# dim out divisor = 1024

[system.io]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/system.io
	# chart type = area
	# type = system
	# family = disk
	# context = system.io
	# units = kilobytes/s
	# priority = 150
	# gap when lost iterations above = 1
	# name = system.io
	# title = Disk I/O (system.io)
	# dim in name = in
	# dim in algorithm = incremental
	# dim in multiplier = 1
	# dim in divisor = 1
	# dim out name = out
	# dim out algorithm = incremental
	# dim out multiplier = -1
	# dim out divisor = 1

[groups.preads]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.preads
	# chart type = stacked
	# type = groups
	# family = disk
	# context = groups.preads
	# units = kilobytes/s
	# priority = 20002
	# gap when lost iterations above = 1
	# name = groups.preads
	# title = User Groups Disk Reads (groups.preads)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 1
	# dim dip divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 1
	# dim slocate divisor = 1024

[mem.pgfaults]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.pgfaults
	# chart type = line
	# type = mem
	# family = system
	# context = mem.pgfaults
	# units = page faults/s
	# priority = 500
	# gap when lost iterations above = 1
	# name = mem.pgfaults
	# title = Memory Page Faults (mem.pgfaults)
	# dim minor name = minor
	# dim minor algorithm = incremental
	# dim minor multiplier = 1
	# dim minor divisor = 1
	# dim major name = major
	# dim major algorithm = incremental
	# dim major multiplier = -1
	# dim major divisor = 1

[netdata.plugin_proc_cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_proc_cpu
	# chart type = stacked
	# type = netdata
	# family = proc.internal
	# context = netdata.plugin_proc_cpu
	# units = milliseconds/s
	# priority = 132000
	# gap when lost iterations above = 1
	# name = netdata.plugin_proc_cpu
	# title = NetData Proc Plugin CPU usage (netdata.plugin_proc_cpu)
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[groups.pwrites]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.pwrites
	# chart type = stacked
	# type = groups
	# family = disk
	# context = groups.pwrites
	# units = kilobytes/s
	# priority = 20002
	# gap when lost iterations above = 1
	# name = groups.pwrites
	# title = User Groups Disk Writes (groups.pwrites)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = incremental
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = incremental
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = incremental
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = incremental
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = incremental
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = incremental
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim dip name = dip
	# dim dip algorithm = incremental
	# dim dip multiplier = 1
	# dim dip divisor = 1024
	# dim gdm name = gdm
	# dim gdm algorithm = incremental
	# dim gdm multiplier = 1
	# dim gdm divisor = 1024
	# dim colord name = colord
	# dim colord algorithm = incremental
	# dim colord multiplier = 1
	# dim colord divisor = 1024
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = incremental
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1024
	# dim netdata name = netdata
	# dim netdata algorithm = incremental
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim root name = root
	# dim root algorithm = incremental
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim mosquito name = mosquito
	# dim mosquito algorithm = incremental
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1024
	# dim slocate name = slocate
	# dim slocate algorithm = incremental
	# dim slocate multiplier = 1
	# dim slocate divisor = 1024

[netdata.server_cpu]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.server_cpu
	# chart type = stacked
	# type = netdata
	# family = netdata
	# context = netdata.server_cpu
	# units = milliseconds/s
	# priority = 130000
	# gap when lost iterations above = 1
	# name = netdata.server_cpu
	# title = NetData CPU usage (netdata.server_cpu)
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.clients]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.clients
	# chart type = line
	# type = netdata
	# family = netdata
	# context = netdata.clients
	# units = connected clients
	# priority = 130100
	# gap when lost iterations above = 1
	# name = netdata.clients
	# title = NetData Web Clients (netdata.clients)
	# dim clients name = clients
	# dim clients algorithm = absolute
	# dim clients multiplier = 1
	# dim clients divisor = 1

[netdata.requests]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.requests
	# chart type = line
	# type = netdata
	# family = netdata
	# context = netdata.requests
	# units = requests/s
	# priority = 130200
	# gap when lost iterations above = 1
	# name = netdata.requests
	# title = NetData Web Requests (netdata.requests)
	# dim requests name = requests
	# dim requests algorithm = incremental
	# dim requests multiplier = 1
	# dim requests divisor = 1

[netdata.net]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.net
	# chart type = area
	# type = netdata
	# family = netdata
	# context = netdata.net
	# units = kilobits/s
	# priority = 130300
	# gap when lost iterations above = 1
	# name = netdata.net
	# title = NetData Network Traffic (netdata.net)
	# dim in name = in
	# dim in algorithm = incremental
	# dim in multiplier = 8
	# dim in divisor = 1024
	# dim out name = out
	# dim out algorithm = incremental
	# dim out multiplier = -8
	# dim out divisor = 1024

[disk_await.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.sda
	# chart type = line
	# type = disk_await
	# family = sda
	# context = disk.await
	# units = ms per operation
	# priority = 2005
	# gap when lost iterations above = 1
	# name = disk_await.sda
	# title = Average Completed I/O Operation Time (disk_await.sda)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[groups.files]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.files
	# chart type = stacked
	# type = groups
	# family = disk
	# context = groups.files
	# units = open files
	# priority = 20050
	# gap when lost iterations above = 1
	# name = groups.files
	# title = User Groups Open Files (groups.files)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1
	# dim slocate name = slocate
	# dim slocate algorithm = absolute
	# dim slocate multiplier = 1
	# dim slocate divisor = 1

[disk_avgsz.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.sda
	# chart type = area
	# type = disk_avgsz
	# family = sda
	# context = disk.avgsz
	# units = kilobytes per operation
	# priority = 2006
	# gap when lost iterations above = 1
	# name = disk_avgsz.sda
	# title = Average Completed I/O Operation Bandwidth (disk_avgsz.sda)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.sda]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.sda
	# chart type = line
	# type = disk_svctm
	# family = sda
	# context = disk.svctm
	# units = ms per operation
	# priority = 2007
	# gap when lost iterations above = 1
	# name = disk_svctm.sda
	# title = Average Service Time (disk_svctm.sda)
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[groups.sockets]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.sockets
	# chart type = stacked
	# type = groups
	# family = net
	# context = groups.sockets
	# units = open sockets
	# priority = 20051
	# gap when lost iterations above = 1
	# name = groups.sockets
	# title = User Groups Open Sockets (groups.sockets)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1
	# dim slocate name = slocate
	# dim slocate algorithm = absolute
	# dim slocate multiplier = 1
	# dim slocate divisor = 1

[disk_await.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.dm_0
	# chart type = line
	# type = disk_await
	# family = dm-0
	# context = disk.await
	# units = ms per operation
	# priority = 2005
	# gap when lost iterations above = 1
	# name = disk_await.dm_0
	# title = Average Completed I/O Operation Time (disk_await.dm_0)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.dm_0
	# chart type = area
	# type = disk_avgsz
	# family = dm-0
	# context = disk.avgsz
	# units = kilobytes per operation
	# priority = 2006
	# gap when lost iterations above = 1
	# name = disk_avgsz.dm_0
	# title = Average Completed I/O Operation Bandwidth (disk_avgsz.dm_0)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[groups.pipes]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.pipes
	# chart type = stacked
	# type = groups
	# family = processes
	# context = groups.pipes
	# units = open pipes
	# priority = 20053
	# gap when lost iterations above = 1
	# name = groups.pipes
	# title = User Groups Pipes (groups.pipes)
	# dim rtkit name = rtkit
	# dim rtkit algorithm = absolute
	# dim rtkit multiplier = 1
	# dim rtkit divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim gdm name = gdm
	# dim gdm algorithm = absolute
	# dim gdm multiplier = 1
	# dim gdm divisor = 1
	# dim colord name = colord
	# dim colord algorithm = absolute
	# dim colord multiplier = 1
	# dim colord divisor = 1
	# dim setroubleshoot name = setroubleshoot
	# dim setroubleshoot algorithm = absolute
	# dim setroubleshoot multiplier = 1
	# dim setroubleshoot divisor = 1
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim mosquito name = mosquito
	# dim mosquito algorithm = absolute
	# dim mosquito multiplier = 1
	# dim mosquito divisor = 1
	# dim slocate name = slocate
	# dim slocate algorithm = absolute
	# dim slocate multiplier = 1
	# dim slocate divisor = 1

[disk_svctm.dm-0]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.dm_0
	# chart type = line
	# type = disk_svctm
	# family = dm-0
	# context = disk.svctm
	# units = ms per operation
	# priority = 2007
	# gap when lost iterations above = 1
	# name = disk_svctm.dm_0
	# title = Average Service Time (disk_svctm.dm_0)
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[disk_await.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.dm_1
	# chart type = line
	# type = disk_await
	# family = /
	# context = disk.await
	# units = ms per operation
	# priority = 2005
	# gap when lost iterations above = 1
	# name = disk_await.dm_1
	# title = Average Completed I/O Operation Time (disk_await.dm_1)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.dm_1
	# chart type = area
	# type = disk_avgsz
	# family = /
	# context = disk.avgsz
	# units = kilobytes per operation
	# priority = 2006
	# gap when lost iterations above = 1
	# name = disk_avgsz.dm_1
	# title = Average Completed I/O Operation Bandwidth (disk_avgsz.dm_1)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.dm-1]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.dm_1
	# chart type = line
	# type = disk_svctm
	# family = /
	# context = disk.svctm
	# units = ms per operation
	# priority = 2007
	# gap when lost iterations above = 1
	# name = disk_svctm.dm_1
	# title = Average Service Time (disk_svctm.dm_1)
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[disk_await.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.dm_2
	# chart type = line
	# type = disk_await
	# family = /home
	# context = disk.await
	# units = ms per operation
	# priority = 2005
	# gap when lost iterations above = 1
	# name = disk_await.dm_2
	# title = Average Completed I/O Operation Time (disk_await.dm_2)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.dm_2
	# chart type = area
	# type = disk_avgsz
	# family = /home
	# context = disk.avgsz
	# units = kilobytes per operation
	# priority = 2006
	# gap when lost iterations above = 1
	# name = disk_avgsz.dm_2
	# title = Average Completed I/O Operation Bandwidth (disk_avgsz.dm_2)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.dm-2]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.dm_2
	# chart type = line
	# type = disk_svctm
	# family = /home
	# context = disk.svctm
	# units = ms per operation
	# priority = 2007
	# gap when lost iterations above = 1
	# name = disk_svctm.dm_2
	# title = Average Service Time (disk_svctm.dm_2)
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[disk_await.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.dm_3
	# chart type = line
	# type = disk_await
	# family = dm-3
	# context = disk.await
	# units = ms per operation
	# priority = 2005
	# gap when lost iterations above = 1
	# name = disk_await.dm_3
	# title = Average Completed I/O Operation Time (disk_await.dm_3)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.dm_3
	# chart type = area
	# type = disk_avgsz
	# family = dm-3
	# context = disk.avgsz
	# units = kilobytes per operation
	# priority = 2006
	# gap when lost iterations above = 1
	# name = disk_avgsz.dm_3
	# title = Average Completed I/O Operation Bandwidth (disk_avgsz.dm_3)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.dm-3]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.dm_3
	# chart type = line
	# type = disk_svctm
	# family = dm-3
	# context = disk.svctm
	# units = ms per operation
	# priority = 2007
	# gap when lost iterations above = 1
	# name = disk_svctm.dm_3
	# title = Average Service Time (disk_svctm.dm_3)
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[disk_await.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.dm_4
	# chart type = line
	# type = disk_await
	# family = dm-4
	# context = disk.await
	# units = ms per operation
	# priority = 2005
	# gap when lost iterations above = 1
	# name = disk_await.dm_4
	# title = Average Completed I/O Operation Time (disk_await.dm_4)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.dm_4
	# chart type = area
	# type = disk_avgsz
	# family = dm-4
	# context = disk.avgsz
	# units = kilobytes per operation
	# priority = 2006
	# gap when lost iterations above = 1
	# name = disk_avgsz.dm_4
	# title = Average Completed I/O Operation Bandwidth (disk_avgsz.dm_4)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.dm-4]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.dm_4
	# chart type = line
	# type = disk_svctm
	# family = dm-4
	# context = disk.svctm
	# units = ms per operation
	# priority = 2007
	# gap when lost iterations above = 1
	# name = disk_svctm.dm_4
	# title = Average Service Time (disk_svctm.dm_4)
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[disk_await.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.dm_5
	# chart type = line
	# type = disk_await
	# family = dm-5
	# context = disk.await
	# units = ms per operation
	# priority = 2005
	# gap when lost iterations above = 1
	# name = disk_await.dm_5
	# title = Average Completed I/O Operation Time (disk_await.dm_5)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.dm_5
	# chart type = area
	# type = disk_avgsz
	# family = dm-5
	# context = disk.avgsz
	# units = kilobytes per operation
	# priority = 2006
	# gap when lost iterations above = 1
	# name = disk_avgsz.dm_5
	# title = Average Completed I/O Operation Bandwidth (disk_avgsz.dm_5)
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.dm-5]
	# history = 3600
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.dm_5
	# chart type = line
	# type = disk_svctm
	# family = dm-5
	# context = disk.svctm
	# units = ms per operation
	# priority = 2007
	# gap when lost iterations above = 1
	# name = disk_svctm.dm_5
	# title = Average Service Time (disk_svctm.dm_5)
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1
